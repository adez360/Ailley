---
tags:
  - character
  - 計畫
status: 規劃中
updated: 2026-08-14
---

# 角色庫與投放

issue #73 的實作計畫。取代 `character_id`/`character_name` 只能靠場景裡固定
`Agent`/`Agent2` 的舊機制，補上「執行期動態生成角色」這條路。

## 定位：這是《00》/《05》生命週期圖的哪一段

`00_設計原則與架構.md` §5 的角色生命週期圖有兩條平行入口，匯流到同一條後續流程：

```
官方模板庫(50個)  或  玩家自建角(面板)
        └──────────┬────────────┘
                    ▼
              建立中(尚未存檔) → 存檔驗證 → 角色生成(system_prompt + words_to_creator)
                    ▼
              已建立・未投放 ◄── 角色庫（靈魂，永久保存）
                    │ 投放（上限5人）
                    ▼
              生成該世界的肉體副本 → 存活
```

**#73 只做左邊那條入口（官方模板庫），而且只接「已經是成品」之後的段落**——
「建立中→存檔驗證→角色生成」這三步的本質是把**尚在編輯的原始輸入**轉成
**驗證過、生成完整**的角色紀錄，模板資料本身應該已經是烘焙好的成品（不管
是誰、什麼時候做的），不需要玩家投放的當下重跑一次。#73 接手的是
「已經是成品的模板」→「進角色庫」→「投放」這一段。

右邊那條入口（玩家自建角色）完整涵蓋「建立中→存檔驗證→角色生成」，
拆成 issue #122，依賴這篇的投放機制跟存檔系統（#21/#22）。

## #73 這次的範圍（MVP）

**做**：
1. 官方模板資料：先用**幾筆佔位資料**（基本身分：`character_id` 留空由
   `_ready()` 生成、`character_name`；`system_prompt`/`words_to_creator`
   先留空字串或假資料）——真正驗證過、AI 生成過 flavor text 的 50 筆模板
   是內容製作工作，不是這次的架構範圍
2. 角色庫：**先用記憶體清單**，不等存檔系統（#21/#22）。模板資料是寫死的，
   重開遊戲不會消失，記憶體清單夠用
3. 投放：`game_manager.gd` 新增 `spawn_character(scene: PackedScene, identity: Dictionary) -> Character`——
   instantiate → 加進場景樹**之前**把 `character_id`/`character_name` 設到
   節點上（`identity` 有給就用，沒給就留空讓 `character.gd::_ready()`
   既有的 fallback 生效）→ `add_child(node, true)`（節點名用 `character_id`，
   UUID 天生不會撞，不靠 Godot 自動改名）
4. `identity` 這次只開兩個 key：`character_id`、`character_name`。
   `ai_provider` 之類的欄位等對應功能真的要做時再加，不預留空欄位
5. 呼叫端：先用一個 debug 指令（例如 `spawn <template_id>`）驗證管線走得通，
   不等 UI

**不做**：
- `Agent`/`Agent2` 維持現狀，不強迫遷移——它們是場景裡手擺的固定角色，
  `npc_schedule.json` 的 assignments 查表模式本來就是為它們設計的
- 存檔／永久保存（依賴 #21/#22）
- `system_prompt`/`words_to_creator` 的真正生成邏輯（依賴 #117 的人格資料
  注入，且 `words_to_creator` 本身是全新的第三種 AI 呼叫類型，跟既有的
  `plan`/`dialogue` 都不同，只在建角完成那一刻打一次——這次先留空）
- 建角面板、角色庫首頁 UI（見 #122）

## 避開的坑（#69/PR #70 留下的教訓）

- 不要讓任何欄位（尤其 `character_name`）被外部系統當 join key 用——
  `village_sim_locale.gd` 那個坑的根源已經被 #86 拔除，但同樣的錯誤模式
  要避免重演
- `character.gd::_ensure_unique_id()` 撞號時的換 id 行為已經驗證過安全，
  不用重寫，`spawn_character()` 直接沿用這套安全網

## 驗收

`filesystem_manage(op="scan")` → `project_run` → `game_eval` 呼叫
`GameManager.spawn_character()` 生一個新角色，確認 `character_id`/
`character_name` 正確、節點確實加進場景樹、跟 `Agent`/`Agent2` 的行為
互不干擾 → `logs_read` 確認無錯誤。

## 相關

- [[Character 基底與 Agent]] —— `character_id`/`character_name` 現有的
  fallback 機制、`_ensure_unique_id()`
- [[LLM 串接與 AI 服務層]] —— `words_to_creator` 這個未來的第三種 AI
  呼叫類型跟 `plan`/`dialogue` 的關係
- issue #122 —— 玩家自建角色這條入口，共用這篇的投放機制
- issue #21/#22 —— 角色庫永久保存依賴的存檔系統
