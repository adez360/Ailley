---
tags: [技術, ui]
status: 已實作
scene: scenes/main_menu.tscn
script: scripts/ui/main_menu.gd
updated: 2026-08-21
---

# 主選單

`run/main_scene` 指向 `scenes/main_menu.tscn`，取代原本開機直接進 `main.tscn`
（遊戲世界）的流程。目前兩個入口：**開始遊戲**、**銘謝**（issue #295）。

具體文案、按鈕位置、排版留給前端組決定，這裡只定義結構與行為。

## 場景結構

```
MainMenu（Control，main_menu.gd）
  Background（ColorRect，全螢幕）
  TitleLabel（遊戲名稱 "Ailley"，寫死不走翻譯——專有名詞）
  ButtonsBox（VBoxContainer，置中）
    StartButton / CreditsButton
  Scrim（ColorRect，全螢幕半透明，預設隱藏）
    CreditsPanel（Setting menu.png 九宮格，樣式沿用 [[UI 版面與素材規格]] 的
                  StatusPanel 做法：region_rect (11,12,106,122)、
                  texture_margin_top=28）
      TitleBg / TitleLabel
      VBox
        QwenLabel / LlamaCppLabel（《16 打包與發布規格書》§2.2 列名的
                                    兩項第三方授權：Qwen2.5 Apache-2.0、
                                    llama.cpp MIT）
        HintLabel
```

## 行為

- **開始遊戲**：`get_tree().change_scene_to_file("res://scenes/main.tscn")`。
  `_ready()` 對 `StartButton` 呼叫 `grab_focus()`，開場就有預設焦點，
  純鍵盤／搖桿也能直接 Enter 開始
- **銘謝**：顯示 `Scrim`（連帶蓋住的 `CreditsPanel`）
- **關閉銘謝**：點面板外或按 `ui_cancel`（Esc／搖桿取消鍵），跟其他面板
  （狀態表、天神之石、販賣機選單、暫停）同一套慣例。
  點面板外的判定不用自訂 hit-test——`Scrim` 蓋滿全螢幕且 `mouse_filter`
  是預設值 `STOP`；`CreditsPanel` 本體同樣預設 `STOP`，但它底下的
  `TitleLabel`／`VBox` 的 Label 群沒有覆寫，走 Godot 4 的 Label／Container
  預設值 `IGNORE`——點擊會先穿過這些裝飾 Label，落到 `CreditsPanel` 本體
  才被吃掉。`Scrim` 收到 `gui_input` 就代表點在面板外，只認滑鼠左鍵
  （`button_index == MOUSE_BUTTON_LEFT`），滾輪 tick／右鍵／中鍵不會誤觸關閉；
  靠 Control 內建的事件冒泡順序解決，不用像 `status_panel.gd` 那樣自己算世界座標

## 翻譯 key

新增於 `locale/game.csv`：`UI_MENU_START`、`UI_MENU_CREDITS`、
`UI_CREDITS_QWEN`、`UI_CREDITS_LLAMACPP`。關閉提示重用既有的
`UI_STATUS_CLOSE_HINT`，不重複造一個一樣的字串。

> [!warning] 改 `locale/*.csv` 後要重 import 才會反映到執行期
> `filesystem_manage(op="reimport")` 對單一 csv 路徑呼叫一次不保證夠——
> 實測跑過一次 `project_run` 後新 key 還是查不到翻譯結果（`TranslationServer.translate()`
> 原樣吐回 key），要先 `filesystem_manage(op="scan")` 做一次完整掃描、
> 再 `reimport` 該 csv，重開一次 `project_run` 才生效。改完務必用
> `game_eval` 呼叫 `TranslationServer.translate("你的新 key")` 實際驗證過
> 不等於原樣吐回，不要只看畫面像不像中文就當作過了。

## 待前端補完

- 版面、配色、按鈕樣式目前是最小可動的骨架，Setting menu.png 素材與
  暖土色調色盤沿用既有慣例，但沒有另外設計
- 沒有「讀取存檔」「設定」等其他常見主選單項目——不在 #295 範圍內，
  之後要加就是新的入口按鈕，接進 `ButtonsBox`

## 相關

- [[UI 版面與素材規格]] —— 9-slice 面板、字型、調色盤的共通約束
