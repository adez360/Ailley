---
tags:
  - 技術
  - character
status: 已實作
scene: scenes/agent.tscn, scenes/player.tscn
script: scripts/character/character.gd, scripts/character/agent.gd, scripts/character/inventory.gd, scripts/character/player.gd, scripts/core/item_database.gd
updated: 2026-08-31
---

# 進食與飲用

`eat()`（#114）／`drink()`（#163）：消耗背包裡一份對應分類的物品，套用該物品在
`Ailley/data/items.json` 定義的效果。兩者完全同一套形狀，`drink` 是照抄 `eat` 做出來的。

## 檔案

| 檔案 | 角色 |
| --- | --- |
| `scripts/character/character.gd` | `eat()`／`drink()` 本體、`_find_food_slot()`／`_find_drink_slot()`、`EAT_*`／`DRINK_*` 失敗原因碼、`apply_personality_delta()` |
| `scripts/character/inventory.gd` | `use_item()`——實際套用 `effect_*` 到 `Stats` 並消耗物品的地方 |
| `scripts/character/agent.gd` | `resolve()` 的硬規則檢查、`_pursue_eat_task()`／`_pursue_drink_task()`；睡眠反思也呼叫 `apply_personality_delta()`（跟物品共用同一套夾制） |
| `scripts/ai/ai_schema.gd` | `IMPLEMENTED_ACTIONS` |

## 核心決策

> [!important] 找物品走 `ItemDatabase` 的 `category`，不是硬編碼白名單
> `_find_food_slot()`／`_find_drink_slot()` 查 `ItemDatabase.get_item(item_id).category`
> 是不是 `"food"`／`"drink"`，不是列一串 `item_id` 比對。物品定義檔（`data/items.json`，#84）
> 加一項新食物或飲品，這裡不用跟著改。

> [!important] 呼叫一次就完成，不走地點式任務的通用追逐路徑
> 跟 `talk` 同一種分流理由（見 [[talk 動作設計]]）：`_pursue_current_task()`
> 通用的地點判斷只會讓角色走去 `params.place` 站著，沒有東西會真的呼叫
> `eat()`/`drink()`。兩者都在 `current_state` 判斷式裡提早分流到各自的
> `_pursue_*_task()`，不進 duration/佔用那條路。

> [!important] `resolve()` 只有硬規則，沒有隨機性
> `eat`／`drink` 不在 `SUCCESS_PARAMS` 上，`resolve()` 對它們只檢查「背包裡有沒有
> 對應分類的物品」，恆定重算結果一樣——跟 `give`／`talk` 同一種「這一類動作不擲骰，
> 前提不成立就是不成立」的處理。

> [!important] 效果套用交給 `Inventory.use_item()`，不是 `eat()`/`drink()` 自己算
> `eat()`/`drink()` 找到物品後把 `ItemDatabase.get_item(item_id)` 整包丟給
> `inventory.use_item(item_id, stats, item)`——它負責讀 `effect_*`（動態對齊
> `Stats.SPEC` 的 8 項數值，不是寫死 4 個）、套用、確認扣格子成功才回傳 OK。
> 「先套效果、扣格子失敗要能回頭」這個順序的保證只有一份實作，不在 `eat()`／
> `drink()` 裡重複一次（見《08》§1-1）。

> [!important] 腐壞（`decay`）由 `use_item()` 一併判定，`eat()`/`drink()` 不重查（issue #840）
> 找到的那份物品 `decay` ≥ 100（完全腐壞）時 `use_item()` 直接回傳 `USE_SPOILED`，
> 不套用任何效果也不消耗物品；40–79（不新鮮）／80–99（快壞了）分別讓 `health`
> 額外扣 -3／-10，跟 `effect_*` 疊在同一次 `stats.add()` 套用（見《08》§6-1）。
> `eat()`/`drink()` 讓 `USE_SPOILED` 對應到專屬的 `EAT_SPOILED`/`DRINK_SPOILED`
> （跟 `Inventory` 共用 "SPOILED" 這個扁平原因碼），不會被兩者原本「其餘失敗
> 一律收斂成 EAT_NO_FOOD/DRINK_NO_DRINK」的既有寫法吞掉——玩家/AI 才分得出
> 「背包沒東西」跟「有東西但腐壞了」的差別。`decay` 本身怎麼隨時間累加見
> [[物品欄]]「腐壞」一節（`Inventory.tick_decay()`，`character.gd::_on_game_minute()`
> 掛進去的那個 tick）。

> [!important] 回復量與酒精效果都在 `Ailley/data/items.json`，不在程式碼常數裡
> 原本的 `EAT_SATIETY_RECOVERY`／`DRINK_HYDRATION_RECOVERY`／`DRINK_ALCOHOL_RECOVERY`
> 三個硬編碼字典已刪除，數字搬進各物品自己的 `effect_satiety`／`effect_hydration`／
> `effect_alcohol` 欄位。含酒精飲品另外帶 `effect_wakefulness`（負值，喝越烈的酒
> 越容易犯睏），這是 2026-08-23 新增的效果，之前沒有酒精影響清醒度的機制。
> `alcohol` 累積後的 `drunk` condition 仍是 `_update_conditions()` 既有的通用
> 門檻邏輯（`alcohol > 30`），不用另外接。

> [!important] 物品也能動人格特質，跟睡眠反思共用同一套夾制（2026-08-23）
> 物品定義可選填 `personality_delta`（格式跟睡眠反思產出的欄位一樣），`eat()`／
> `drink()` 成功後呼叫 `Character.apply_personality_delta()` 套用，跟 `agent.gd`
> 睡眠反思那邊改呼叫同一支函式——夾制公式只有一份。反思那邊的數字有 `ai_schema.gd`
> 的 ±3 驗證（防 LLM 自己講過頭），物品這邊是開發者寫在靜態資料裡的數字，不套用
> 同一道驗證。現有物品都還沒有填這項。

## 玩家手動使用（`use_selected_item()`，#611）

`eat()`／`drink()` 是「自動找背包裡第一個符合分類的物品」，服務 AI 角色的任務
執行器；玩家要的是「用我快捷欄選的那一格」，兩者语意不同，所以另開一支函式，
不是讓 Player 呼叫 `eat()`/`drink()`（2026-08-27 拍板）。

`Character.use_selected_item()`：讀 `inventory.get_selected_index()` 對應的格子，
依 `ItemDatabase` 的 `category` 決定 `is_consumable`（`food`／`drink` 為 `true`，
其餘一律 `false`），直接傳給 `inventory.use_item()`——是否可用完全交給
`Inventory` 自己的 `USE_NOT_CONSUMABLE` 判斷，`use_selected_item()` 不重新
判一次分類白名單。失敗原因碼 `NO_INVENTORY`／`NO_SELECTION`（沒選格或選到
空格）／`NO_STATS`／`IS_DEAD` 是自己的，其餘（`NOT_CONSUMABLE`／`INVALID_EFFECT`／
`REMOVE_FAILED`…）原樣轉傳 `Inventory.USE_*`，跟 `buy_from()` 轉傳
`NOT_ENOUGH`／`NO_SPACE` 同一個理由。

> [!important] `is_dead` 要自己查，不能指望呼叫端先擋
> 死屍呼叫 `use_selected_item()` 沒有任何地方會被自動擋下——`is_dead=true`
> 之後沒有任何機制停用玩家的 `_unhandled_input()`（PR #615 CodeRabbit review
> 抓到）。跟 `talk_to()` 擋自己是死屍發起搭話同一種漏洞、同一種修法：函式
> 一開頭直接查 `is_dead`，不是等 `Inventory`／`Stats` 幫忙擋。這裡刻意只查
> `is_dead`、不查 `_is_movement_locked()`（涵蓋昏迷／治療）——CodeRabbit 這次
> 只點名死亡，昏迷/治療中能不能用道具留給之後真的有人問的時候再決定，
> 不在這裡順手擴大範圍。

`player.gd::_unhandled_input()` 新增 `use_item` 鍵位（綁 **R**），跟 `make_noise`
同一種優先序——放在 `interact` 攔截鏈之前，對話中或販賣機選單開著都能觸發，
不用擠進那條鏈。按下後失敗一樣走 `report_action_failure()`。

Agent 沒有輸入路徑，不需要對應的按鍵處理；`use_selected_item()` 掛在
`Character` 而不是 `Player`，是因為 `get_selected_index()` 本來就是
`Inventory` 的資料層狀態（見 [[物品欄]]），Agent 沒有 UI 但仍有「手上拿著
什麼」的概念，之後若要讓 Agent 也能「用手上選的東西」不用重寫。

## 未做

無（`eat`／`drink`／玩家手動使用三條路徑都已接上）

## 相關

- [[物品欄]] —— `Inventory` 容器本身，`get_summary()`／`remove_item()` 這兩層 API
- [[talk 動作設計]] —— 「呼叫一次就完成」動作跟通用地點式任務分流的先例
- [[Ailley]] —— 筆記庫入口
