---
tags:
  - 交流
status: 現況
updated: 2026-08-21
---

# 五則 issue 拍板與 PR 收尾（2026-08-21）

各 PR 的拍板結論已經寫進對應規格書（見下方各列 PR），這則只追蹤 PR 進度，
全部合併後可以刪除。

## 處理的 issue

| Issue | 內容 | PR | 狀態 |
| --- | --- | --- | --- |
| #392 | 庫存容量數值訂正（§06） | #425 | ✅ 已合併 |
| #370 | 每角色一間房子分配規則 | #447 | 🟡 CodeRabbit 重新審查中，意見已處理並回覆 |
| #369 | 化身後天神之石行為 | #449 | 🟡 checks 已過，reviewDecision 待翻盤，意見已處理並回覆 |
| #368 | 死亡系統全部拍板項 | #451 | 🟡 CodeRabbit 重新審查中，意見已處理並回覆 |
| #394 | `_pursue_eat_task()` 補 `_reevaluate()` | #456 | ✅ 已合併（過程中連帶修正 `_pursue_drink_task()` 同一個收尾模式） |

## 過程中新開的 issue

- **#454**：`agent.gd` 有兩組重複的 `get_save_data()`／`load_save_data()`（PR #431 與 #429 各自獨立加的），導致遊戲完全開不起來——已修、已合併
- **#475**：懷疑 `game_manager.gd` 的 `apply_world_save_data()` 也有類似的重複 `character_library` 載入區塊——查證後發現已被別的 PR（#466）修過，關閉標重複

## 待決事項

- [ ] #447／#449／#451 等 CodeRabbit 重新審查翻盤成 APPROVED 後即可自行合併（意見都已處理並回覆，沒有新的待辦）
- 過程中發現有另一條平行工作線同時在動 `note/規格書/09_死亡屍體與墓園.md`（新增了「無名碑是顯示規則、資料不會被清空」的說明），未跟這批工作協調過——#451 合併前留意有沒有衝突
