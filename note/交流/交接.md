---
tags:
  - 交流
status: 現況
updated: 2026-08-23
---

# 交接（2026-08-23，全局重新整理）

給下一個 session 看的總覽。這則做完該做的事、待決事項都轉移完之後就可以刪除——
不要從別的地方引用它。

## 開始前：先開 worktree

**這個 session 會跟原本那個 session 同時跑**，兩邊共用同一份
`D:\Projects\Ailley` 工作目錄會互相干擾（切分支、寫檔都會讓對方看到的
檔案內容跟著變）。開始動作前先呼叫 `EnterWorktree` 開一份獨立的檔案系統
副本，不要直接在原目錄下操作。

## 這次要做什麼

使用者想做一次**全局重新整理**：把專案現況重新盤點過，重新定一個目標（要不要
擴充到 Phase 3／4、擴充多少）。

**這題的確切範圍還沒跟使用者敲定**，開新 session 後先直接問清楚，不要自己假設。
討論到一半時，我提過兩種可能的框架給使用者選，但使用者還沒選就決定先交接：

1. **輕量刷新**：`17_產品範圍與完整度地圖.md` 上一輪已經把 17 份規格書、
   《99》待拍板清單、實際程式碼三方比對過一次，並排好 Phase 1-4 優先序——
   只需要確認這段期間新合併的 commit 有沒有讓 17 的現況描述過期，
   有落差的地方修正，然後跟使用者確認 Phase 優先序還算不算數。成本低。
2. **從零重新盤點**：不信任 17 上一輪的結論，17 份規格書、全部程式碼、
   所有 GitHub issue 重新交叉比對一遍。成本高很多，可能要開多個 subagent
   分頭讀。

開新 session 後第一件事：把這兩個選項（或使用者這次想到的別的框架）攤開來問，
不要預設走哪一條。

## 已知會影響判斷的資訊

- `17_產品範圍與完整度地圖.md` 是上一輪 session（2026-08-22/23）新增的文件，
  三個軸的分工見文件開頭：《00》＝規格寫了沒、《99》＝個別問題拍板了沒、
  《17》＝功能真的做進遊戲了沒。
- 17 寫成之後，`main` 又合併了 20 個 commit（`git log` 範圍
  `eb37a26..28efe96`，本 session 已把本地 `main` fast-forward 到
  `28efe96`）。裡面至少這幾個看起來會動到 17 的完整度地圖，需要重新確認：
  - `#441` `feat/buy-agent-execution`——買東西的 AI 執行流程
  - `#449` `design/god-stone-post-embodiment`——天神之石在化身者接上後的調整
    （17 的「天神之石」列已經提過 `#377 OPEN`，這個可能是相關/後續）
  - `#520` `feat/dialogue-call-cap`、`#491` `fix/chat-input-turn-state`——對話系統
  - `#440` `feat/sleepy-condition-effects`——睏倦狀態效果
  - `#527` `feat/packaging-prep-ai-config-and-licenses`——17 的「打包發布」列
    寫著這則 PR「已合併」，可以核對是否屬實、有沒有連帶更新缺口描述
- 17 裡「目前位置」一段提到死亡與墓園系統剩 `#387`／`#388`／`#390`／
  `PR #526`（移除死碼）——這幾個现在的狀態需要重新查一次，17 寫的時候
  是「待實作／待合併」。
- 已知 17 本身有一處過期：附註提到「餐酒館／藥草鋪的收購禁令（issue #141）
  什麼時候解」，但 `#141` 其實在 2026-08-19 就關閉了（決策：收購機制暫不做，
  等生產鏈一起設計，見 commit `5c32a0a`）。這處我還沒來得及在本 session
  修掉，新 session 可以順手處理，也可以當作「17 需要重新核對」的一個例子。

## 跟這個 session 平行進行、不要重複

本 session（同一個 repo，另開的視窗／稍後繼續）已經走完 17 附註列的
**Stage 1 決策輪**（6 個地點容量數字定案、村莊廣場獨立開新地點、布告欄
確認身兼兩用途），寫回規格書＋開了 PR #546（本則筆記所在的這個 PR）。
新 session 不用管這塊，避免兩邊互相覆蓋。

本 session 目前也在處理 PR #545（`use_item()` 通用化）與本 PR 的
CodeRabbit review，兩者跟這次全局整理無關；新 session 開始前建議先
`git switch main` 或直接開新的 worktree，不要接著這兩條分支做。

## 相關

- 《17_產品範圍與完整度地圖》—— 上一輪整理的成果，這次重新檢查的對象；
  目前只存在於 PR #545（`feat/item-effect-generalization`）分支上，還沒
  merge 進 main。連結在 main 上暫時是死的，PR #545 merge 後才會生效
- [[99_待規劃項目清單]] —— 個別問題的拍板追蹤
- [[Ailley]] —— 筆記庫入口
