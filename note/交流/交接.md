---
tags:
  - 交流
status: 現況
updated: 2026-08-23
---

# 交接（全專案現況與規劃總盤點，進行中）

這是本次「全專案盤點」任務的工作記錄，**盤點完、結論都抄回對應規格書／技術／ai
筆記後整份刪除**——不要從別的地方引用它。取代了同名的舊交接筆記（上一輪「全局
重新整理」的中斷點，內容已被這次任務取代）。

工作區：`D:\Projects\Ailley\.claude\worktrees\docs-full-project-audit`，分支
`worktree-docs-full-project-audit`，base 為 `origin/main`（`049656a5`，含 PR
#545／#526）。跟原本 `feat/task-switch-log` 分支的髒工作目錄完全無關，不要互相碰。

## 範圍更正

使用者原本講「14 份規格書」，實際 `note/規格書/` 有 21 份頂層文件＋`07_地點/`
15 篇子筆記＋新增的《17》＝37 份。全部涵蓋，不卡在數字落差上。

## 執行順序與進度

- [x] **00** 設計原則與架構 —— 已修正：補回遺漏的《15》索引列；13 狀態從
      「現況」改回「大致定案」（跟《99》P-27 對齊）；新增「追蹤類文件」附註，
      正式承認 17／99 用 `現況（追蹤類）`，不用六個定案程度硬套
- [x] 順手處理 **issue #547**（status 詞彙不統一）：13/14/17/99 四份 frontmatter
      同步修正，`note/Ailley.md` 說明也補一句，已留言說明結論並關閉
- [x] **06** 資料欄位對應表 —— 已修正兩個真的過期的欄位描述：`home_location_id`
      宣稱「引擎已依此欄位解析 loc_home」，實際只存在 SQLite schema，JSON 存檔線與
      `character/*.gd` 都還沒接（#391 未 commit）；`appointment.with/location/game_time`
      宣稱已有條件式 schema 機制，實際程式碼只有 `expires_in_minutes` 這個簡化替代，
      結構化 `appointment` 物件尚未接進決策 schema（#479 OPEN）——**這兩處在查 10／12
      時要順手一併修正**，10§5.5／12§2.4 目前的寫法可能也在描述同一個還沒實作的機制。
      其餘欄位（10 項 personality、8 項 physical、36 格背包容量）逐一 grep 對過，
      名稱數值都跟程式碼一致，沒有問題
- [x] **01／01-1／01-2／01-3** —— 01／01-1 逐條 grep 對過（10 項 personality 公式、
      HEXACO 6→10 轉換式、`personality_delta` 物品欄位）全部跟程式碼一致，沒有問題；
      01-2 修正一處真的過期的地方：`status: 數值待填` 早就該是「已定案」（99 的
      P-04 已經☑，§3 表格數字也全部填好了），順手補了更精確的實作現況——
      `_roll_success()`／`SUCCESS_PARAMS` 六筆都已照公式寫好，但 `hunt_small`／
      `hunt_large`／`gather`／`fish`／`steal`／`perform` 六個動作全部不在
      `ai_schema.gd` 的 `IMPLEMENTED_ACTIONS` 白名單裡，LLM 選不到，公式目前
      沒有任何真實路徑會打中；`struggle` 在白名單但還沒接上 haul 檢查點（#337）。
      00 的索引表 01-2 那列也同步改成「已定案」。01-3 沒發現問題
- [x] **02** 狀態情緒與流程 —— 全部核對過（8種情緒enum、狀態機、中斷規則、
      MIN_ACTION_DURATION=10分鐘），跟程式碼完全一致，沒有修改
- [x] **03** 事件評估與記憶 —— 記憶欄位、分級門檻常數（DISCARD_BELOW/L3_AT/L4_AT）
      跟 memory.gd 一致，沒有修改
- [x] **04** Godot 與 AI 資料介接規格 —— 修正兩處：①`appointment` 條件式 schema
      （§4-2 回應範例、§8 約束表）跟 06 一樣尚未實作，已改成現況＋指向 #479；
      ②逾時秒數：規格書寫 LocalLLM 5秒/RemoteLLM 15秒/event 8秒分開，但
      `ai_config.gd` 的 `DEFAULT_TIMEOUT` 其實是單一全域 10 秒（HTTPRequest
      節點池共用，程式碼註解說明是刻意設計，不是遺漏）——這條順手回填到
      99 的 P-11 決策紀錄。物理數值換算（hunger=100-satiety 等）、POOL_SIZE=3
      都核對正確沒問題
- [ ] **07** 地點與行動 ＋ `07_地點/` 15 篇
- [ ] **08** 物品與經濟
- [ ] **09** 死亡屍體與墓園
- [ ] **10** 模式連線與世界設定
- [ ] **11** 人際互動與社交行為
- [ ] **12** 決策來源抽象與執行架構
- [ ] **05** 玩家角色塑造面板
- [ ] **13** 系統分析與系統設計（總驗收）
- [ ] **14** 存檔資料存取層 —— 已知要重查：`決策.md`「存檔架構分工」讀起來比
      「方案待選」更接近已決定（JSON 為主、SQLite 非阻塞選配），frontmatter 這次
      先套用《00》既有記法，內容本身的定案程度留到查這份時再確認要不要再調
- [ ] **15** 遊戲內 UI 規格
- [ ] **16** 打包與發布規格書
- [ ] **17** 產品範圍與完整度地圖 —— 待用前面查到的結果解掉它自己列出的已知落差
      （Phase 分期對齊、UI 三面板完成度、issue #141 過期）
- [ ] **99** 待規劃項目清單（最後一輪）
- [ ] 技術筆記／ai 筆記 status／updated 抽查（穿插在對應規格書之後）
- [ ] open issue／PR 全列表三分類（已有基礎清單，見下方）
- [ ] 核對四份常駐文件（專案現況／決策／計畫／後續待辦）
- [ ] 刪除 `交流/規格書審核清單.md`
- [ ] 盤點完，刪除這則

## 已抓到的基礎清單（供最後彙整用，不用重查）

- open issue 47 則、open PR 12 則，清單見任務一開始的 `gh issue list`／`gh pr list`
  輸出（本檔沒有複製一份，需要時重新跑指令即可，都是即時資料不值得存快照）
- PR 現況粗篩：`#546`／`#543`／`#489` APPROVED＋MERGEABLE；`#538`／`#532`／`#519`
  APPROVED 但 CONFLICTING（需 rebase）；`#548`／`#530`／`#529` CHANGES_REQUESTED；
  `#540`／`#533` CHANGES_REQUESTED＋CONFLICTING；`#511` APPROVED＋CONFLICTING
  （這條屬於 `feat/task-switch-log`，跟這次任務所在的目錄無關，但一併列入現況）
- `issue-batch-交接.md`（另一批 session 處理 #477/#391/#455/#384）跟這次任務
  無關，不刪不動，除非查證時發現它描述的狀態已過期

## 相關

- [[99_待規劃項目清單]]
- [[17_產品範圍與完整度地圖]]
- [[Ailley]]
