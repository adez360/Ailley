1. 該遊戲`主模式`及`AI模式`皆可以多人聯機
2. 一名`player`的主機最多同時運行2個`agent`（暫定）
3. `主模式`有完整的生活空間，包括家園、商店、工作....(原計劃書)
4. `AI模式`為觀察`agent`自主生活的模式，和`主模式`的唯一區別在`player`不可在此模式操作
5. `AI模式`和`主模式`共用`角色存檔`及`世界存檔` (角色存檔包括`player`和`agent`)
6. `角色存檔`保存所有`player`和`agent`的存檔，可以被多個`世界文檔`讀取
7.  `世界存檔`保存地圖資訊