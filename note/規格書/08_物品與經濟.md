---
tags: [規格書, 世界模擬]
status: 大致定案
updated: 2026-08-16
---

# 08_物品與經濟

# 08｜物品與經濟

**閱讀對象**：遊戲邏輯組・後端組
**前置閱讀**：《00 設計原則與架構》、《07 地點與行動》

> [!note] MVP 範圍
> 本文件為完整版方向。MVP 階段的簡化做法見《13》§三（2026-08-16 已核過）。MVP 兩間店（餐酒館／藥草鋪）各自賣什麼見下方「MVP 販售品項」小節。

## 0. MVP 販售品項（2026-08-16，篩自 §3 既有清單）

依 §5「商鋪對應」既有的官方分類（藥草鋪＝藥品＋藥草湯，餐酒館＝食物＋飲品），MVP 兩間店各賣這些：

| 地點 | MVP 上架（定案） | 之後再加 |
| --- | --- | --- |
| 餐酒館 | `ale` 麥酒（串連 `alcohol`/`drunk` 機制，最有戲劇性）、`cooked_meat` 烤肉（飢餓回復最高）、`water` 水（最低價剛需）——3 樣各涵蓋一種機制，MVP 只上這 3 樣（2026-08-16 拍板） | `bread`／`fish_dish`／`spirit`（§3 已有價格與效果，只是暫不上架） |
| 藥草鋪 | `medicine` 藥品（降低 `injury`，見下）、`herb_soup` 藥草湯（輕量食療） | — （§3 目前只有這 2 項符合「藥品＋藥草湯」分類，全上） |

> [!note] 兩間店只做「販售」，不做「收購」（2026-08-19 拍板，見 [issue #141](https://github.com/adez360/Ailley/issues/141)）
> §5 商鋪對應表寫兩間店都有「收購」（餐酒館收獵物/魚、藥草鋪收藥草/野果），但貨源地（`loc_forest`／`loc_lake`／`loc_herb_field`）都不在 MVP 的 5 個地點裡，MVP 沒有 `hunt`／`fish`／`gather` 這幾個生產行動可用，角色沒有東西可以賣。**不承諾之後一定會做**：收購要不要做，等生產鏈（打獵/採集/釣魚地點與行動）真的要設計時再重新評估，不是現在訂下的路線圖。

> MVP 用得到的部分（§0／§4 堆疊／§5 交易規則／§6 腐壞懲罰）已於 2026-08-16 拍板定案，
> 追蹤於《99》§文件確認狀態。標記 `【推測待確認】` 的段落是完整版才需要的內容（§3 完整
> 18 項物品清單、§7 資源再生），MVP 用不到，不阻塞。
> 

---

## 1. 物品結構

```json
{
  "item_id": "herb",
  "count": 4,
  "decay": 22,
  "durability": 100
}
```

| 欄位 | 型別 | 範圍 | 說明 |
| --- | --- | --- | --- |
| `item_id` | string | — | 物品代號 |
| `count` | int | ≥ 0 | 數量 |
| `decay` | int | 0–100 | 腐壞值，**100 = 完全腐壞** |
| `durability` | int | 0–100 | 耐久值，**0 = 損毀** |

### 兩個數值的差別

|  | `decay` 腐壞 | `durability` 耐久 |
| --- | --- | --- |
| 方向 | 越高越糟 | 越低越糟 |
| 隨時間變化 | ✔ 自動上升 | ✘ 只在使用時下降 |
| 適用 | 食物、飲品、獵物、水產、採集品 | 隨身用品 |
| 歸零／滿值時 | 不可食用，`eat` 判定失敗 | 損毀，自動移除 |

---

## 2. 物品分類

| 分類 | 英文 | 使用 `decay` | 使用 `durability` | 說明 |
| --- | --- | --- | --- | --- |
| 食物 | `food` | ✔ | ✘ | 提升 `satiety` |
| 飲品 | `drink` | ✔ | ✘ | 提升 `hydration`；酒類提升 `alcohol` |
| 小型獵物 | `small_game` | ✔ | ✘ | 打獵產出，可販售或烹煮 |
| 大型獵物 | `large_game` | ✔ | ✘ | 同上，價值較高 |
| 水產 | `seafood` | ✔ | ✘ | 捕魚產出 |
| 採集品 | `gathered` | ✔ | ✘ | 藥草等 |
| 隨身用品 | `carry` | ✘ | ✔ | 刀、樂器、衣物等 |

---

## 3. 物品清單　【完整版待確認】

以下為依既有討論推導的最小可用清單。**MVP 實際只用得到 `medicine`／`herb_soup`／`ale`／
`cooked_meat`／`water`（見 §0，已定案）**——獵物水產、採集品、大部分隨身用品都綁著森林／
藥草叢／湖泊／服裝鋪等不在 MVP 5 個地點內的地方（`打獵`／`演奏` 這些對應行動也延後），
下面 3-3～3-5 的內容留給完整版，不阻塞 MVP。

### 3-1 食物 `food`

| `item_id` | 中文 | 飢餓回復 | 腐壞速率（/tick） | 售價 |
| --- | --- | --- | --- | --- |
| `bread` | 麵包 | -25 | +0.3 | 12 |
| `cooked_meat` | 烤肉 | -40 | +0.5 | 25 |
| `fish_dish` | 魚料理 | -35 | +0.5 | 20 |
| `herb_soup` | 藥草湯 | -20 | +0.8 | 15 |

### 3-2 飲品 `drink`

| `item_id` | 中文 | 口渴回復 | `alcohol` | 售價 |
| --- | --- | --- | --- | --- |
| `water` | 水 | -40 | 0 | 2 |
| `ale` | 麥酒 | -20 | +25 | 10 |
| `spirit` | 烈酒 | -10 | +45 | 22 |

### 3-3 獵物與水產

| `item_id` | 中文 | 分類 | 腐壞速率 | 售價 |
| --- | --- | --- | --- | --- |
| `small_game` | 小型獵物 | `small_game` | +0.8 | 30 |
| `large_game` | 大型獵物 | `large_game` | +0.6 | 90 |
| `fish` | 魚 | `seafood` | +1.0 | 25 |

### 3-4 採集品 `gathered`

| `item_id` | 中文 | 腐壞速率 | 售價 | 備註 |
| --- | --- | --- | --- | --- |
| `herb` | 藥草 | +0.4 | 18 | 採收後需等待才會再生 |
| `wild_fruit` | 野果 | +0.9 | 8 |  |

### 3-5 隨身用品 `carry`

| `item_id` | 中文 | 效果 | 耐久消耗 | 售價 |
| --- | --- | --- | --- | --- |
| `knife` | 刀 | 打獵成功率 +10% | 每次使用 -2 | 120 |
| `good_knife` | 好刀 | 打獵成功率 +20% | 每次使用 -1 | 350 |
| `instrument` | 樂器 | 演奏成功率 +15% | 每次使用 -1 | 200 |
| `clothes_basic` | 一般衣物 | — | 每日 -1 | 60 |
| `medicine` | 藥品 | `injury` -30，下限夾制 0，不會變負值（2026-08-16，見《99》P-28；降到 20 以下 `bleeding` 會自動消失，見《02》§2-2） | 一次性 | 45 |
| `battery` | 電池 | 劇情道具，見《00》Flag 系統 | — | 不可交易 |

---

## 4. 容量與儲存

| 位置 | 欄位 | 上限 | 說明 |
| --- | --- | --- | --- |
| 隨身 | `economy.inventory` | **36 格**（快捷欄 9 ＋ 主背包 27，單一容器） | 一格 = 一種物品，可堆疊，見 [[物品欄]] |
| 家中 | `economy.home_storage` | **50 格** | 僅在 `loc_home` 可存取，尚未實作 |

### 堆疊規則

| # | 規則 |
| --- | --- |
| 1 | 同 `item_id` 且 `decay` 差距 ≤ 10 時可堆疊，取較差的 `decay`（2026-08-16 拍板） |
| 2 | 單一堆疊數量上限 **30 個**，隨身與家中儲存皆適用；超過上限要開新格，新格不足則同 #4 |
| 3 | `carry` 類（有 `durability`）**不可堆疊**，一件佔一格 |
| 4 | 格數滿時無法取得新物品，判定失敗，`reason` 寫「你身上已經沒有位置了」 |

---

## 5. 經濟循環

```
        ┌─────────────────────────────────────┐
        │              收入                    │
        │  打獵 → 獵物 → 賣給商鋪              │
        │  採集 → 藥草 → 賣給藥草鋪            │
        │  捕魚 → 魚   → 賣給商鋪              │
        │  演奏 → 路人或玩家給錢                │
        └──────────────┬──────────────────────┘
                       ▼
                  ┌─────────┐
                  │  money  │  預設 300
                  └────┬────┘
                       ▼
        ┌─────────────────────────────────────┐
        │              支出                    │
        │  餐酒館 → 食物、酒                   │
        │  藥草鋪 → 藥品                       │
        │  服裝鋪 → 衣物                       │
        │  工具   → 刀、樂器                   │
        └─────────────────────────────────────┘
```

### 商鋪對應

| 地點 | 收購 | 販售 |
| --- | --- | --- |
| `loc_herb_shop` | 藥草、野果 | 藥品、藥草湯 |
| `loc_clothing_shop` | 衣物 | 衣物 |
| `loc_tavern` | 獵物、魚 | 食物、飲品 |

### 交易規則

規則 1、2 是**收購**專用——MVP 兩間店只做販售不做收購（MVP 範圍 2026-08-16 拍板；
不承諾未來是否會加，2026-08-19 拍板，見上方 §0 note），這兩條 MVP 不適用，留給完整版；
規則 3、4 已於 2026-08-16 拍板採用，MVP 直接用。

| # | 規則 | MVP |
| --- | --- | --- |
| 1 | 商鋪收購價 = 售價 × 0.5 | 不適用（無收購） |
| 2 | `decay` 越高收購價越低：實際收購價 = 基準 × (100 − decay) / 100 | 不適用（無收購） |
| 3 | `reputation < -50` 時商店拒絕交易——這是社會性限制，`buy` 仍留在 `available_actions` 裡讓 AI 能選，由引擎判定失敗（`reason` 回「老闆看你的樣子不想做你的生意」），不像缺物品／狀態不允許那樣直接從清單過濾掉，見《07》§5 | 適用 |
| 4 | 商鋪庫存與現金為無限，不做商鋪經營模擬 | 適用 |

---

## 6. 腐壞與耐久

### 6-1 腐壞

```
每 tick：decay += 該物品的腐壞速率

decay ≥ 100 → 標記為腐壞
```

| `decay` | 狀態 | 效果 |
| --- | --- | --- |
| 0–39 | 新鮮 | 正常 |
| 40–79 | 不新鮮 | 食用時 `health` -3（2026-08-16 拍板） |
| 80–99 | 快壞了 | 食用時 `health` -10（2026-08-16 拍板） |
| 100 | 腐壞 | 不可食用，`eat` 判定失敗 |

> `home_storage` 的腐壞速率減半（2026-08-16 拍板）——家裡比背包涼快。
> 

### 6-2 耐久

```
每次使用：durability -= 該物品的耐久消耗

durability ≤ 0 → 損毀，自動從庫存移除
```

損毀時寫入 `last_action_result`：「你的刀在使用中斷掉了。」

---

## 7. 資源再生

> [!note] MVP 不適用
> 這 5 種資源全部產自森林／藥草叢／湖泊，都不在 MVP 5 個地點內（見《13》§三），MVP 沒有
> `hunt`／`gather`／`fish` 這幾個行動可用。本節維持完整版待規劃，不阻塞 MVP。

| 資源 | 地點 | 再生時間（tick）【推測待確認】 |
| --- | --- | --- |
| `herb` | `loc_herb_field` | 36（6 遊戲小時） |
| `wild_fruit` | `loc_herb_field` | 72 |
| `small_game` | `loc_forest` | 36 |
| `large_game` | `loc_forest` | 144（1 遊戲日） |
| `fish` | `loc_lake` | 24 |

採集或捕獲後，該資源進入冷卻，冷卻期間執行對應行動一律失敗，`reason` 寫「這裡的藥草被採光了，得等它們長回來。」

---

## 8. 本文件待確認項目

MVP 用得到的部分（§0／§4／§5 規則 3-4／§6-1）已於 2026-08-16 拍板定案，見《99》P-18 決策紀錄。
剩下都是**完整版才需要**、不阻塞 MVP 的部分：

| 章節 | 項目 | 追蹤 |
| --- | --- | --- |
| §3 | 完整 18 項物品清單、效果數值、售價（MVP 只用 §0 的 5 項） | 《99》P-18 |
| §5 規則 1-2 | 收購價比例、`decay` 折價公式（MVP 不做收購） | 《99》P-18 |
| §7 | 資源再生時間（MVP 沒有對應的採集/打獵/捕魚地點） | 《99》P-18 |