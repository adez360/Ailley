---
tags: [規格書, 角色]
status: 大致定案（MVP 走方案 A，B/C 留待之後）
updated: 2026-09-02
---

# 01-3_Prompt注入與資料傳送

# 01-3｜Prompt 注入與資料傳送

**閱讀對象**：AI 組・後端組
**文件性質**：決定「每次呼叫 LLM 要餵什麼進去」的規格。
**前置閱讀**：《00 設計原則與架構》原則二、原則三

---

## 1. 注入分級

| 級別 | 內容 | 頻率 | 估計 token |
| --- | --- | --- | --- |
| **System**（固定） | 行為準則 2~4 條 ＋ `character` ＋ 外觀文字 | 建角後不變，可命中 KV 快取 | ~250 |
| **Tier 0**（常駐） | 生理 8（附形容詞）＋ `emotion` ＋ `conditions` ＋ `current_goal` ＋ `today_plan` ＋ `last_action_result` ＋ 事實句 | 每次決策請求 | ~300 |
| **Tier 1**（情境） | 在場角色的 `met_count`（含快取外觀描述，見下方）、在場物品、可用行動清單 | 依情境 | ~350 |
| **Tier 2**（按需） | 完整庫存、指定對象完整關係、記憶檢索、地點細節 | AI 要求才給 | ~300 |
| **不注入** | `personality` 10 項、`hexaco_input` 6 項、`words_to_creator` | — | 0 |

> `words_to_creator` 平時不注入。它是角色對「自己被設定成這樣」的一句吐槽，角色本人不會反覆想起它。
**唯一例外**：玩家用天神之石對它說話且觸發擲骰成功時，該次 prompt 會額外注入一行「你突然很想說出這句話：『…』」，由 AI 自行決定要不要真的說出口。詳見《01》§1-4。
> 

> [!note] 他人外觀的注入時機（見《99》P-08）
> 跟自己的外觀（System 段，逐字元固定）不同，別人的外觀不整段重注：初次相遇時注入一次
> （一句話長度的外觀描述，跟自己的 `appearance` 文字同等級），引擎把它存進該角色對這個人的
> `relations` 記錄裡（一個簡短欄位，不是完整長期記憶）。之後同場，Tier 1 帶出這個人的
> `met_count` 資料時，順便帶出這個快取的外觀描述（`appearance_cache`），不用整段外觀文字重注。
> `appearance_cache` 目前沒有任何呼叫端寫入，「隨 Tier 1 帶出」是目標設計，尚未實作。
> 外觀會變動（換裝、受傷等），這個快取需要對應的更新機制，另案追蹤（issue #498）。
> 角色的 `conditions`（`injured`／`filthy` 等）只出現在自己的 `_self_block()`，別人的
> `conditions` 從未被傳給看到他的人——外觀描述不承載即時狀態。

## 2. 既有注入原則

| # | 原則 |
| --- | --- |
| 1 | **固定筆數的資料全給**（生理 8、L1 記憶 8 條、L4 核心記憶） |
| 2 | **不定筆數的才過濾**（`conditions` / L2、L3 記憶 / 在場人物 / 事件 / 可用行動） |
| 3 | 過濾一律由**引擎**判斷，不交給 LLM |
| 4 | **狀態不可靜默省略**。若省略，必須加兜底句 |

### 兜底句範例

| 省略了什麼 | 兜底句 |
| --- | --- |
| `conditions` 為空 | 你身上沒有值得一提的異常。 |
| 在場無人 | 這附近沒有其他人。 |
| 記憶檢索無結果 | 你想不起相關的事。 |

---

## 3. 事實句

引擎在組 prompt 時依條件產生**陳述句**，不設欄位、不指定情緒。這是取代 `stress` / `loneliness` / `satisfaction` 的機制。

> [!note] 9 條裡已接 7 條（2026-08-24 核對，見 `Ailley/scripts/character/agent.gd::_fact_lines_summary()` 註解）
> `persuade` 待回應、社交沉默三級距、目標拖延、首次造訪、連續同一動作失敗都已實作。
> **「正被搬運中」與「昏迷中曾被搬運，醒來後首個 tick」這兩條還沒接**——依賴 `haul`
> 執行層，範圍界線見 #338。
> 原 9 條清單之外後來新接的：跟隨狀態（issue #576，條件持續成立就持續注入）、
> 昏迷倒數（issue #803）。

| 觸發條件 | 注入文字 |
| --- | --- |
| 距上次社交 ≥ 30 tick（5 小時） | 你已經五個小時沒和任何人說過話了。 |
| 距上次社交 ≥ 60 tick（10 小時） | 你已經大半天沒和任何人說過話了。 |
| 距上次社交 ≥ 120 tick（20 小時） | 你快一整天沒有和任何人說過話了。 |
| `current_goal` 連續 36 tick 未達成 | 你想做的那件事已經拖了很久還沒完成。 |
| 該地點首次造訪 | 你以前沒有來過這裡。 |
| 連續 3 次同一行為失敗 | 這件事你已經連續搞砸三次了。 |
| 正被 `haul` 搬運中（清醒） | 你正被人搬離原地，動彈不得。 |
| 昏迷中曾被 `haul` 搬運，醒來後首個 tick | 你發現自己不在原本待的地方，好像被人搬到了這裡。 |
| 曾被 `persuade`，尚未回應 | 剛才〔說服者〕試著說服你〔理由，若對方希望你做某件具體的事，一併描述那件事〕，你被說動了嗎？（見《01-2》§3-1，行動說服／純思想說服的區分見 issue #227） |
| 昏迷倒數期間的每次決策（issue #803） | 你已經陷入昏迷，失去行動能力。若 N 遊戲分鐘內沒有獲得救助（被搬離或治療），你將死亡。（N 為剩餘倒數，隨時間遞減；上限見《09》「死亡與昏迷的關係」） |

> [!note] 社交沉默三級門檻全部落在最早的生理危機之前（issue #797）
> 門檻以 tick 為單位（1 tick = 10 遊戲分鐘）：30/60/120 tick = 5/10/20 遊戲小時。
> #731 調慢 drift 後，生理數值從出生跌破 CRITICAL(30) 最快也要 143 tick
> （stamina／wakefulness；hydration ≈ 180 tick、satiety ≈ 252 tick），三級敘事
> 在危機前就完整走完、不會跟生理危機撞在一起——沉默敘事在角色還能自由
> 行動時登場，生理危機一旦發生，就由 condition／reflex 接手主導決策。

### 撰寫規則

| ❌ 不要 | ✅ 要 |
| --- | --- |
| 「距離上次社交：60 tick」 | 「你已經大半天沒和任何人說過話了。」 |
| 「你覺得很孤單」 | （不寫感受，只寫事實） |
| 「孤獨度 72」 | （不寫數值化的感受） |

> 本地小模型對過於中性的平述句反應偏弱。事實句要口語化、有畫面，但**絕不能替角色下結論**。
> 

### 資料來源（不新增任何 NPC 欄位）

| 事實句 | 來源 |
| --- | --- |
| 多久沒說話 | `last_social_tick`（引擎全域狀態） |
| 目標拖延 | `goal_set_tick`（引擎全域狀態） |
| 首次造訪 | 該角色的地點造訪紀錄 |
| 連續失敗 | `last_action_result` 歷史 |
| 正被搬運中 | `haul` 進行中既有的搬運狀態記錄 |
| 醒來後發現位置變了 | 比對昏迷前後位置是否不同（既有位置資料） |
| 剛被說服 | `Agent._pending_persuade`（引擎新增：說服者、理由、選填的 `proposed_task`，回應後即清除，見《01-2》§3-1）；同一時間只保留一筆，已有待回應記錄時新的說服直接判定失敗，不覆蓋 |
| 昏迷倒數 | `Character.get_incapacitation_remaining_minutes()`（昏迷狀態機既有計時器換算，見《09》「昏迷倒數的提示」） |

---

## 4. 分層傳送機制

> [!note] MVP 走方案 A（2026-08-16 定案）
> MVP 不做情境預測式分層，先固定全量傳送，換取零額外實作成本與可預期的行為；
> 方案 B 列入下一版主線，C 更後面再評估（見《99》P-03）。
**問題**：每次決策請求全量注入約 1,100~1,300 token，成本隨決策請求次數累積（決策頻率由事件驅動，見《00》§3/§7）。本地模型 context 16000 單次沒問題，但**速度與 AI 注意力聚焦度**是真正的成本。

### 4-1 三個方案

|  | **A：固定全量** | **B：情境預測式** | **C：兩段式按需** |
| --- | --- | --- | --- |
| 誰決定給什麼 | 不決定，全給 | **引擎**依地點與上一動作決定 | **AI** 自己要求 |
| 呼叫次數 | 1 次 | 1 次 | 1~2 次 |
| token 降幅 | — | 約 40~60% | 約 50~70% |
| tick 延遲 | 基準 | 不變 | 最壞情況翻倍 |
| 需改 GBNF | 否 | 否 | **是**（新增 `request_info`） |
| 需改 DES 時序 | 否 | 否 | **是** |
| 實作成本 | 無 | 中（維護對照表） | 高 |
| 風險 | AI 注意力分散 | 引擎誤判導致缺資料 | 小模型濫用或不會用 |

### 4-2 方案 B 運作方式

```
當前情境 ──► 查對照表 ──► 決定 Tier 1 內容

┌──────────────────┬────────────────────────────────┐
│ 情境              │ 帶入的 Tier 1 資料              │
├──────────────────┼────────────────────────────────┤
│ 在商店 / 交易中   │ 金錢、完整庫存、商品清單         │
│ 有他人在場        │ 在場者 `met_count`、可社交行動  │
│ 在森林 / 獨處     │ 地點資源、可工作行動（不帶關係） │
│ 剛被攻擊 / 目擊   │ 對象關係、可用防禦行動           │
│ 睡醒第一個 tick   │ today_plan 模板、昨日摘要        │
└──────────────────┴────────────────────────────────┘

缺資料時 ──► 加兜底句（符合 §2 原則 4）
```

### 4-3 方案 C 運作方式

```
Pass 1：Tier 0 + Tier 1 + 一個 request_info 欄位
   │
   ├─ AI 回傳 request_info: []            ──► 直接進判定
   │
   └─ AI 回傳 request_info:
        ["inventory", "relation:npc_003"]  ──► 引擎補 Tier 2
                                           ──► Pass 2（上限 1 次）
                                           ──► 進判定
```

### 4-4 建議

**採 B 為主線，C 列入迭代二。**

理由與死線直接相關：B 是純引擎邏輯，不動 LLM 介面，對照表可漸進調整、隨時回退全量；C 需要改 GBNF grammar 與呼叫流程，且會影響 DES 佇列時序，在 8/21 前導入風險偏高。

決策追蹤於《99》P-03。

---

## 5. 不需決策、建議直接做的優化

`system_prompt` 段固定不變 → 在 llama-server（`--parallel 3`）下每個 slot 各自的 KV cache 可直接命中。

**條件**：system 段永遠排在 prompt 最前面且**逐字元一致**（含空白與換行）。

這一項純收益、沒有取捨，建議直接列入實作。

### 組裝順序（固定不可調換）

```
┌─────────────────────────────────────────────────┐
│ ① System 段（永不變，可快取）                  │  行為準則 + character + 外觀
├─────────────────────────────────────────────────┤
│ ② Tier 0（資料每 tick 更新、每次決策請求注入） │  生理 + 情緒 + 目標 + 上次結果 + 事實句
├─────────────────────────────────────────────────┤
│ ③ Tier 1（依情境）                             │  在場關係 + 可用行動
├─────────────────────────────────────────────────┤
│ ④ 輸出格式指示                                 │  GBNF 已約束，此段可精簡
└─────────────────────────────────────────────────┘
```

---

## 6. 輸入格式的兩個必要條件

| # | 條件 | 為什麼 |
| --- | --- | --- |
| 1 | **數值一定要附中文形容詞** | `satiety: 62` 對模型沒有基準，「飢餓 62（很餓）」才有 |
| 2 | **一定要回報上一個動作的失敗原因** | 沒有原因，AI 會重複同一個錯誤直到永遠 |