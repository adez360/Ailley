---
tags:
  - ai
status: 參考
updated: 2026-08-25
---

# 決策紀錄

AI 專用密集格式參考。**《99_待規劃項目清單》拍板後的完整決策帳本**，2026-08-25 從《99》
搬出——《99》只留「還沒 ☑ 的 P 編號才有完整背景與選項」，已經 ☑ 的 P 編號在《99》只留一兩行
「已定案，結論見哪份文件」，完整的「為什麼這樣決定、查證過程發現了什麼」敘述都在這裡。

**用途**：考古用。想知道某個 P 編號或某個 issue 當初怎麼拍板的、查證過程發現了什麼旁支問題，
來這裡查；平常追蹤「還有什麼沒決定」看《99》狀態總表就夠，不用讀這份。

**維護方式**：append-only，新決策從《99》移過來時加在最下面，不回頭改舊條目的內容——
沿革本身就是這份文件存在的理由，不像技術筆記要求「只寫現況」。

---

| 日期 | 編號 | 決策內容 | 決策者 | 已同步文件 |
| --- | --- | --- | --- | --- |
| 2026-08-24 | P-23 | 群體對話排最後的依賴安全性確認：核對過 MVP 其餘項目對群體對話零依賴，可安全排最後。唯一要注意的是 `ai_schema.gd`／`prompt_builder.gd` 兩個檔案表演打賞（P-62）也會加欄位，各自加自己的欄位不衝突，但動工前建議先 rebase 一次避免同檔案 git merge 衝突 | 你 | 99 |
| 2026-08-24 | P-18／P-39／**P-62** | 打獵／採集／表演三個簡化版工作系統拉進 MVP（不含捕魚）。物品清單擴為 9 項——`small_game`／`large_game`／`herb`／`instrument` 四項早已在《08》完整 18 項清單定案（售價 30／90／18／200），這次只是啟用不是新設計；森林／藥草叢的資源再生時間沿用《07》既有值（36／144／36 tick）。打獵的「追逐」簡化為單次成功率判定，不做即時追逐互動。表演打賞明確排除引擎隨機金額（同 PR #489 的原則二問題），改為 NPC 自己的 AI 決定，細節登記 **P-62** | 你 | 17 / 99 |
| 2026-08-24 | 《17》MVP 範圍最終定案（專題發表版，取代同日稍早的版本，**本身也已被同日下一列 P-18／P-39／P-62 決定取代**：`perform`／打獵／採藥後來還是拉回 MVP，這裡的排除清單以下一列為準；**自動化測試／新手引導後來也拉回 MVP，見《17》本文的 2026-08-24 拍板段落，以《17》現況為準**） | 確認這是**專題發表要交付的範圍**，跟「產品完整版最終要做到哪」分開。收斂為：地基／生理心理（含記憶完整版）／世界行動（含餐酒館藥草鋪正式互動流程，沿用現有 5 項物品，不擴充到 18 項）／社交（含群體對話，排最後處理）／UI／化身者與死亡系統／下載版打包（單一平台，驗證 Local/Cloud AI 都能連線；不含網頁版——瀏覽器擋 localhost 連線，會讓 Local AI 展示不出來）／模組化手動測試計畫（P-61）。**明確排除移出 MVP**（同日稍早版本曾納入，這次收斂拿掉）：16 個地點裡除餐酒館藥草鋪外其餘 9 個地點建置、`perform`／打獵／採藥／《08》完整物品清單、社交規範完整化（report/偷竊/洗心革面所）、連線與多人、發布完整版（三平台 CI／簽章／Steam／網頁版）、非玩法產品完整度（音效/新手引導/自動化測試）——理由是團隊人力（C 目前 UI 進度落後，這批多半需要 UI 或設計都還沒拍板）與專題發表時程，不是產品不需要這些，全部移入未來願景。群體對話例外拉回 MVP：C 澄清她只做視覺精修不寫程式碼，程式碼可以給 T／R，UI 瓶頸疑慮解除 | 你 | 17 / 99 |
| 2026-08-24 | 《17》MVP 範圍重新拍板（已被上一列取代） | 拿掉 Phase 1~4 框架，MVP 改成一份不分版的攤平清單。拍板拉進 MVP：記憶完整版（L3/L4 語意檢索）、16 個地點全部建置（結婚禮堂機制除外）、正式工作與經濟系統（`perform`／打獵／採藥／完整版餐酒館與草藥鋪／《08》完整物品清單）、社交規範完整化（`report`／偷竊／洗心革面所，不含群體對話八卦聲望）、連線與多人、打包發布、非玩法產品完整度（音效／新手引導／自動化測試）、新增模組化手動測試計畫（**P-61**）。明確保持未來：婚禮機制（`loc_chapel` 地點仍建置，機制留後續）、角色外觀完整 slot 系統（P-01）、地圖擴張與同居（P-58）、群體對話／八卦／聲望連動（P-23，跟 report/偷竊分開判斷，架構改動量大，見 conversation.gd 分析） | 你 | 17 / 99 |
| 2026-08-24 | P-60 (issue #353／PR #489) | PR #489 原本要把「深度對話+2／被攻擊-50／被救助+15」三個固定公式接上 `relations.trust`，全專案盤點判定違反《00》原則二（引擎幫事件定性成該加減多少信任，AI 沒機會表態）——已改為事實句陳述（攻擊、救助各自的 `_on_attacked()`／新增的 `_on_rescued()` 掛點；深度對話不需要額外事實句），拿掉固定公式加減，`add_trust()` 恢復零呼叫端。`relations.trust` 欄位／DB schema／存檔格式本身要不要整個拿掉，留給 **P-60** 另外決定，不夾帶在這次修復裡 | 你 | 99 / PR #489 |
| 2026-08-24 | issue #477（PR #530） | `preconditions` 的 `field` 拿掉 `relations.<key>`（trust／met_count）命名空間支援，只剩 `stats.<key>`——允許用信任度濾候選任務等於引擎在 AI 看到任務前就依關係狀態濾掉候選，跟《07》§5「社會性限制不過濾，AI 能選、由引擎判定失敗並給理由」的既有方向相反。改為 fail-closed（跟其他不認得的命名空間同一條路） | 你 | 99 / 技術/行程佇列與任務仲裁 / PR #530 |
| 2026-08-24 | issue #405（PR #538） | `_on_lost()` 跟丟反應拿掉 `has_met()` 的「已認識才回報」閘門——引擎依關係狀態決定要不要讓 AI 知道跟丟這件事，本身就是一種定性標籤（見 CLAUDE.md 新增的「不回報也是一種標籤」段落）。改為每位角色每天對每個對象最多觸發一次（`_lost_reacted` 逐日重置），不分是否已認識 | 你 | 99 / 技術/視覺感測 / PR #538 |
| 2026-08-21 | issue #382 | `epitaphs`（一對多，SQLite）沿用 #124 骨架留下的 `GraveEpitaphSchema.gd` 既有結構（`grave_id`＋`npc_id` 複合唯一鍵，FK 分別指向 `grave`／`npc`，跟 `GraveHighlightSchema.gd` 同一種寫法），不重新設計；只補上《規格書 09》§4-5 拍板的 `content` 字數上限 40（`CHECK (LENGTH(content) <= 40)`），`DatabaseSchema.CURRENT_VERSION` 加到 4（3 已被 #446 的 NOT NULL 主鍵修復佔用）、新增對應 migration 重建既有資料庫的這張表。P-50「要不要移除死亡系統死碼」不含 `GraveEpitaphSchema`，只剩 `GraveSchema`／`GraveHighlightSchema`／`NPCDeathSchema` 待決。CRUD（`SqliteSaveService` 讀寫）尚未接，等死亡系統實際落地再做 | Wendy | 99 / 技術/存檔 |
| 2026-08-21 | issue #451 | 墓園滿 6 格：Phase 2 用最簡化版——拒絕新安葬與自動立無名碑，屍體留在原地繼續佔一般 capacity，是已知限制。Phase 3 方向拍板：動態擴張墓園＋超過 6 格後每 7 個合併成一個列表格顯示（資料不刪減，只換顯示方式），登記 **P-57** 待實作。另外拍板「擦拭墓碑」新行動：任何角色可做、無次數限制，把無名碑的 `is_anonymous` 設回 `false`——無名碑立碑當下 `name`／`last_words`／`life_highlights` 就已經整份寫進 `grave` 紀錄，只是面板不顯示，擦拭不是找回資料，只是恢復顯示 | 你 | 09 / 99 |
| 2026-08-20 | issue #370 | 房子間數跟著房主設定的世界人數上限走（MVP／Phase 2 剛好 5 間對齊 5 個 NPC，不會不夠用）；分配方式採循環（round-robin），游標跟房屋佔用狀態一起存檔，不靠最大索引反推。地圖動態擴張與同居功能延後到 Phase 3/4，見新增 P-58 | 你 | 07_地點/家 / 99 |
| 2026-08-20 | issue #368 | Phase 2 死亡系統：昏迷改為死亡前置狀態（不再是取代關係），`health ≤ 0` 先觸發昏迷、依傷勢動態計算時長，逾時未獲救治才轉真正死亡；純觀察模式天神可主動介入送醫。連帶拍板：`last_words` 各死因是否有機會留言（瞬間死亡也有，但是「心裡想著」而非口白）、屍體佔用地點容量、搬運可被中斷、屍體腐壞速率 0.7/tick、悼詞上限 40 字、墓園容量 6（固定值）、復活付費機制（固定金額、已下葬者同條件可復活）、真人角色death規則取消不對稱（改跟 AI 完全同一套，唯一保留真人不受《10》§8.3 世界禁入限制）。實作細節（昏迷公式、UI、復活金額具體數字）留給後續 issue | 你 | 09 / 技術/昏迷狀態機制 / 99 |
| 2026-08-20 | issue #381 | `last_words`／`words_to_creator`／`life_highlights` 三個墓碑欄位接進 `Agent.get_save_data()`／`load_save_data()`（JSON 路徑）：`words_to_creator` 舊存檔缺欄位時保留現有值（比照 `character_id`），`last_words`／`life_highlights` 缺欄位一律重置成空（比照 `memory` 的「讀檔要跟檔案內容一致」規則）。過程中發現 `database/schemas/` 已有更早一輪（#124）留下、跟這次分工衝突的 SQLite grave/death schema，這次不動，登記 **P-50** 待決策 | Wendy | 99 / 技術/存檔 |
| 2026-08-20 | issue #373 | 世界存檔新增 `embodied_character_id`，跟 `allow_player_join` 同一層，存檔時從場景 `"player"` 分組即時算出來、讀檔時寫回 `GameManager.embodied_character_id`。讀檔當下若場景裡的 player 節點跟存檔記錄的不同，只 `push_warning`、不自動換身——真正的換身機制依賴 #371（化身者投放路由），這則只接存讀路徑不動 | Wendy | 99 / 技術/存檔 |
| 2026-08-20 | P-38 #3（issue #369） | MVP-2 玩家化身後，天神之石說話功能鎖住，只有純觀察者（含純 AI 模式）能用；化身後改成能在石頭旁查看新設計的「地點事件記錄」（所有玩家透過天神之石說過的話，不含角色反應），獨立於圖書館與 `words_to_creator` 之外，不提前揭露、不併入圖書館的固定節點寫入模型 | 你 | 07_地點/天神之石 / 15_遊戲內 UI 規格 / 99 |
| 2026-08-20 | P-33／P-38 #4（issue #369） | 玩家（天神視角）恆知天神之石真實座標，不受角色「需親眼目擊才更新已知位置」機制限制——這是玩家側跟角色側刻意不同步的設計，角色仍然得自己去發現 | 你 | 07_地點/天神之石 / 99 |
| 2026-08-19 | P-44（issue #297） | `database/` 底下資料庫腳本搬到 `scripts/database/`，對齊「腳本一律進 `scripts/<領域>/`」規則；`project.godot` 的 `DatabaseManager` autoload 路徑與 `Ailley/scripts/database/DatabaseManager.gd` 內僅有的一處硬編碼字串路徑同步改掉 | Wendy | 99 / `project.godot` / `技術/存檔` |
| 2026-08-19 | P-48（issue #239） | `Ailley/scripts/database/DatabaseSchema.gd` 的 `initialize()` 改用 `PRAGMA user_version` 版本化 migration（PR #303），取代原本「版本不符直接拒絕啟動」；`MIGRATIONS` 已有 1 項（version 2，issue #270／P-46 的 `water`／`ale` 腐壞數值 backfill），之後再出現不相容 schema 變更時同樣加項即可 | Wendy | 99 / `DatabaseSchema.gd` / 技術/存檔 |
| 2026-08-17 | P-45 | `npc_daily_plan` 不加 `UNIQUE (npc_id, game_day)`，同一天允許多筆——`today_plan` 是不限筆數的「想做的事」清單而非排定行程，一列對應清單一項，跟 `npc_taboo`／`npc_inventory`／`memories` 同屬「delete 全部再整批 insert」的一對多表，`sqlite_save_service.gd` 原本的分類正確 | 你 | 99 |
| 2026-08-16 | P-32 | `hunger` range 定為 0–100、方向改成「越高越飽」，跟 `stats.gd` 對齊；`NPCStateSchema.gd` 的 hunger 欄位同步改成 0–100；《02》starving 門檻與死亡規則同步反向。連帶決定：注入給模型時換算成「飢餓度」百分比＋中文形容詞並存（見 P-07） | 你 | 01 / 02 / 99 / `NPCStateSchema.gd` |
| 2026-08-13 | 00§3/10§4.1 | 時間流速固定為 1:60（現實 1 秒＝遊戲 60 秒），可調整機制列入待辦（P-25） | 你 | 00 / 10 / 12 / 99 |
| 2026-08-15 | P-26 | 發表衝刺（3 天／10 人規模）確定用 JSON，`SaveService` 介面落地；SQLite 平行開發改為非阻塞、選配，不影響發表時程 | 你 | 14 / 99 |
| 2026-08-16 | 13§三 | MVP 範圍逐項核過：世界單機跑，角色驅動來源做 Local／Cloud／Human 三種；5 個地點（家/藥草鋪/餐酒館/涼亭/天神之石）；工作簡化為單一工作站、餐酒館沿用販賣機機制；外觀簡化為造型組；死亡簡化為昏迷（可搬運，細節見 P-27）；社交先做 talk/give/persuade/shout，perform 與 report 延後 | 你 | 13 / 99 |
| 2026-08-16 | P-27/28/29 | 昏迷送醫改自動送藥草鋪（暫定90分鐘）、搬運可中斷（原地放下）；攻擊必中、暫定傷害與流血數值；移動速度更正為已實作值、新增跑步機制追蹤 | 你 | 07 / 02 / 99 / 技術/talk動作設計 / 07_地點/藥草鋪 |
| 2026-08-16 | 06 | 新增 `decision_source`／`model_name` 欄位，記錄角色由哪個模型驅動，供角色面板顯示；顯示時機（是否常駐顯示）延後至 P-30 | 你 | 06 / 13 / 99 |
| 2026-08-16 | 08 | 篩出 MVP 餐酒館／藥草鋪販售品項；收購方向因缺產地暫緩，先只做販售 | 你 | 08 / 99 |
| 2026-08-16 | P-27/28 | `medicine` 也止血；藥草鋪治療統一成一個動作，解除所有異常狀態（含昏迷、`bleeding`）；`bleeding` 不加下限保護；昏迷觸發門檻 MVP 只做 `health ≤ 0`，保留未來擴充空間 | 你 | 02 / 08 / 99 / 07_地點/藥草鋪 |
| 2026-08-16 | P-29 | 跑步機制（含玩家操作與 AI 自主判斷）整個延後到 MVP 之後，MVP 期間一律用基礎速度 | 你 | 07_地點與行動 / 99 / issue #140 |
| 2026-08-16 | P-16/06 | 建角面板決策來源選雲端沿用既有 `AIConfig`，不新增金鑰欄位；`decision_source`／`model_name` 投放後不可改（套用 personality 同規則） | 你 | 06 / 99 |
| 2026-08-16 | P-17 #12 | 家固定幾間，建角時系統自動分配，玩家不用選 | 你 | 07_地點/家 / 99 |
| 2026-08-16 | P-23 | `murmur`（自語）採純機率觸發，跟 `idle` 平行 | 你 | 99 |
| 2026-08-16 | 08§0 | 餐酒館 MVP 只上架 `ale`／`cooked_meat`／`water` 3 樣（各涵蓋一種機制），`bread`／`fish_dish`／`spirit` 延後 | 你 | 08 / 99 |
| 2026-08-16 | P-14 | 《02》情緒與狀態機制全部定案：8 種情緒（12 種草案作廢）、持續時間公式、`conditions` 清單／疊加／中斷規則全採用；長動作中止採固定間隔檢查，MVP 唯一長動作 `work` 中止不撥款；同步修正 §5 死亡判定與 MVP 昏迷矛盾 | 你 | 02 / 99 |
| 2026-08-16 | P-15 | 《03》記憶參數全部定案（L4 上限、分級門檻、衰減公式、酒精遺失判定式）；§7 語意檢索確認屬完整版功能，MVP 不用，維持待規劃 | 你 | 03 / 99 |
| 2026-08-16 | P-23 | 《11》社交行動清單與 2 人對話限制確認跟《13》§三一致，非新決策；群體對話等留在 §3 成長方向，不阻塞 MVP | 你 | 11 / 99 |
| 2026-08-16 | P-13 | 極端值文案改傾向式語氣，只給偏好不規定行為；做 3 種語氣變體，36 條（12 組×3）已定稿 | 你 | 01-1 / 99 |
| 2026-08-18 | P-37 | `SqliteSaveService` 接上真正的 CRUD（issue #147）：新增 `world`／`world_character_state` 兩張表補上世界層 schema 缺口；`get_character`／`save_character` 對齊 `Character.get_save_data()` 目前實際吐出的簡單形狀（`character_id`／`character_name`／`stats` 11 項扁平數值／`relationships`），不是《06》完整版 identity/hexaco/personality/economy/state.emotion/memory 那份——後者現在沒有任何 Character 欄位在產生，接了也沒有呼叫端能餵資料。已知缺口（`npc_state` 缺 `mood`／`social`／`fun`、`npc_relations` 缺 `met_count`）維持用預設值頂著，登記在 `sqlite_save_service.gd` 檔尾註解，不建新表 | 你 | 技術/存檔 / 99 |
| 2026-08-16 | — | `detained`（拘留）維持刻意的懲罰設計，不改成 AI 可選擇脫逃/求饒——這是規格書裡唯一明講剝奪 AI 自由行動的機制，確認為刻意保留，非疏漏 | 你 | — |
| 2026-08-16 | P-31 | 對話聽者要有對稱退出點，每輪讓聽者回傳要不要繼續聽，不再只能靠移動側門離開；schema／呼叫配額／逾時互動等細節待補 | 你 | 技術/talk動作設計 / 99 |
| 2026-08-16 | P-17 | 《07》地點與行動全部定案：15 個地點清單、四大類行動分法、B/C/D 行動清單、偵測半徑（視覺5/聽覺3/大叫8/天神之石6）全採用；MVP 5 個地點的 `danger`/`resources`/`regen` 統一填 0/空/空；跨村移動確認 MVP 不適用（單一村莊） | 你 | 07 / 07_地點/家、藥草鋪、餐酒館、涼亭 / 99 |
| 2026-08-16 | P-18 | 《08》堆疊規則、商鋪無限庫存、腐壞懲罰（40-79→-3/80-99→-10）、家中儲存減半腐壞全採用；收購價比例／`decay`折價公式／完整18項物品清單／資源再生時間確認屬完整版，MVP 不用（不做收購、沒有對應採集地點） | 你 | 08 / 99 |
| 2026-08-16 | P-19 | 09（死亡屍體墓園）8 項待決策超過門檻，先跳過不處理；MVP 已用昏迷取代真正死亡，這份很可能大半不適用於 MVP，待之後正式確認 | 你 | 99 |
| 2026-08-16 | P-16 | 05 面板 UI 細節全部定案：滑桿極端區變色、提供「整個角色隨機」按鈕（組合分頁2/3現有隨機邏輯）、角色庫顯示雷達圖、角色庫容量上限改為15個（原推測無上限）、三分頁建角流程；P-01 外觀 slot 系統 6 項確認 MVP 不擋路，維持完整版才需要——七份含推測文件全部處理完畢 | 你 | 05 / 99 |
| 2026-08-16 | P-02/05/19/24/21/25 | 逐項核對《13》§三後確認完整版才需要、MVP 不擋路，先跳過：正式職業系統、名聲變動數值、死亡與復活全套、議程/回放/跨世界成長、可調時間流速 | 你 | 99 |
| 2026-08-16 | P-03/04/09/10 | MVP Prompt 傳送走方案 A（固定全量）；行為時間與成功率全部採建議值（`persuade` 新增 10 分鐘）；`words_to_creator` 天神之石觸發機率 25%（情緒強度≥70 時 40%）、不消耗機會 | 你 | 01-3 / 99 |
| 2026-08-16 | P-06 | `morality`／`greed`／`honesty` 不移除、不另做議價/說謊公式，經《01-1》§4 傾向文案進 `system_prompt`（跟其餘 7 項同一套消費機制）；`morality` 未來接 `report` 意願判定，等《洗心革面所》一起做 | 你 | 99 |
| 2026-08-16 | P-11/04架構 | 修正《04》錯誤架構描述：刪除獨立「AI 後端（Python）」這層，改為 Godot `AIService` 直連 `LocalLLM`（llama-server 8080）／`RemoteLLM`；`/decide` 逾時 `LocalLLM` 5 秒／`RemoteLLM` 15 秒、`/event` 8 秒、併發上限 5、JSON 重試 1 次、`protocol_version` 1.0、`TICK_BUDGET` 對齊逾時值（P-22）；llama-server 隨遊戲啟動自動拉起 | 你 | 04 / 99 |
| 2026-08-16 | P-27 修正 | `haul` 搬清醒角色的「半敵對，關係中幅下降」公式整段刪除：引擎內部用固定公式替動作定性、再寫進 `relations` 供其他系統當客觀事實使用，本質上跟直接注入情緒標籤是同一件事，違反原則二精神，《00》原則二與 CLAUDE.md 自我審查規則同步補充這一點；`haul` 本身不再自動改變 relations，關係要不要變交給當事人後續行為決定。清醒角色被搬運中新增環境事實句告知「正在被搬運」（不歸因是誰），昏迷中被搬運者醒來後也新增一句，見《01-3》§3 | 你 | 00 / 07 / 01-3 / CLAUDE.md / 99 |
| 2026-08-16 | P-27 補完 | 被搬運清醒角色的 `available_actions` 新增 `idle`（不反抗任由搬運），不再被迫二選一；`struggle` 判定頻率改掉「每 2 秒重骰」（會在 `haul` 一個 tick 內瘋狂重複發起決策，跟呼叫成本衝突），改成搭在 `haul` 的長動作固定間隔檢查點上（見《02》§3），每次檢查點觸發時搬運者與被搬運者**同時**各自收到一次決策請求；《02》§3 機制同步擴充，明講檢查點不限於動作發起者自己 | 你 | 02 / 01-2 / 07 / 99 |
| 2026-08-16 | P-20 #5 | 內建本機模型定案 Qwen2.5-7B-Instruct（沿用現有程式碼與規格書範例值），量化等級待效能實測後補 | 你 | 10 / 99 |
| 2026-08-16 | P-22 | 真人短逾時 30 秒／中逾時 120 秒（逾時視同離線）；雲端驗證失敗重試維持 2 次；§8 簡化路線與 `appointment` 細節留待實測/P-21 | 你 | 99 |
| 2026-08-16 | P-12 | 採用修正式解開 `greed`／`courage` 的綁死（各多混一個 hex 維度），同步進《01-1》§3；不加建角面板「進階」微調區；50 個官方模板跟玩家角色走同一套 6→10 轉換，不跳過 | 你 | 01-1 / 99 |
| 2026-08-16 | P-08 | 他人外觀初次相遇注入一次，快取進 `relations.*.appearance_cache`（新欄位），之後隨 Tier 1 關係資料帶出，不用整段重注；MVP 無換裝機制，不需要更新機制 | 你 | 01-3 / 06 / 99 |
| 2026-08-16 | P-10 | 生成指示文案確認已在《01》§1-4 定稿；GBNF 約束 ≤60 字與型別；內容違規重試上限 3 次，仍失敗改用固定備用句庫；50 個官方模板資料準備階段就先生成、人工檢閱 | 你 | 04 / 99 |
| 2026-08-16 | P-07 | 8 項生理數值×5 級距中文形容詞對照表定稿；`hunger` 這欄注入的是換算過的飢餓度（`100−hunger`），不是原始值；跟《02》既有 `starving`/`dehydrated`/`exhausted` 用詞對齊，不重複造詞 | 你 | 01 / 04 / 99 |
| 2026-08-16 | P-32 追加 | 需求型生理欄位（會自然消耗、需玩家維護）全部統一「越高越好」，理由是 `stats.gd` 的 `get_lowest_need()`/`needs_attention()` 直接拿數值比大小找最糟需求，方向不一致會讓這個共用機制比錯。`hunger`→`satiety`、`thirst`→`hydration`（越高越好）、`sleepiness`→`wakefulness`（越高越好）；`stamina`/`hygiene`/`health` 方向本來就對，不改名。`alcohol`/`injury` 是事件累積型，不參與 `get_lowest_need()`，故意維持「越高越差」，不強行統一。注入給 LLM 的顯示值（飢餓度/口渴度/睡意）維持原本「越高越不好」的直覺方向不變，只是換算來源欄位改名 | 你 | 01 / 02 / 04 / 06 / 99 / `stats.gd` / `NPCStateSchema.gd` |
| 2026-08-16 | P-27/28 收尾 | 重新檢視《99》先前抓的暫定量級並拍板：攻擊傷害（`health` -15／`injury` +20）、`bleeding` 衰減（每 tick -1.5，不設下限）維持原暫定值——**此獨立衰減規則同日已被下方「P-28 整合」取代，`bleeding` 改為 `injury ≥ 20` 推導的門檻狀態，`injury` 達門檻時自然衰減（-0.5/tick）暫停**；藥草鋪治療時間 90→**60 遊戲分鐘**縮短；搬運速度 70%→**50%**、搬運體力消耗 -2→**-3／tick** 調重，兩項皆改成「搬人是件苦差事」的量級。P-27／P-28 全部項目正式定案，狀態總表與詳細段落同步改 ☑ | 你 | 02 / 07 / 07_地點/藥草鋪 / 99 |
| 2026-08-16 | P-27 通用化 | 搬運從「只搬昏迷者」擴大成通用機制：新增 `carryable` 旗標（07 §1-1）、`haul`（搬運）與 `struggle`（掙脫）兩個新動作（07 §2-4、01-2 §3）。強制搬運清醒角色視為半敵對行為（比 `attack` 輕，關係中幅下降，掙脫成功降幅減半）——**此關係下降規則同日已被上方「P-27 修正」撤銷，`haul` 本身不再自動改變 `relations`，這裡僅保留當初通用化搬運機制的完整背景**；單人搬運可被 `struggle` 掙脫（基礎 30%，套用通用成功率公式），雙人搬運完全無法掙脫。天神之石納入 `carryable`，但因與《10》§7「位置性須保留」的既定精神有衝突，改設計成「搬走後需親眼目擊才能更新已知位置，抵達後對不上真實座標則判定失敗並清空已知位置」，架構已定，數值與細節另開 **P-33** 追蹤 | 你 | 01-2 / 07 / 07_地點/天神之石 / 10 / 99 |
| 2026-08-16 | P-28 整合 | `bleeding` 從「攻擊額外觸發、只能手動清除」的獨立旗標，整合成純粹由 `injury ≥ 20` 推導的門檻自動狀態，跟其餘 6 個生理衍生狀態同一套機制；`attack` 現在只做 `injury +20` 一件事。為保留「流血不會自己好」的設計意圖，改成 `injury ≥ 20` 時自然衰減（-0.5/tick）暫停，治療只需要把 `injury` 打下門檻，`bleeding`／`injured` 自動跟著消失。順帶把《02》§2-2 全部生理／社會／終局狀態标上「門檻自動／倒數計時／事件手動」三種套路之一，之後加新狀態（如生病）先選套路、填參數，不用另外設計機制 | 你 | 01 / 02 / 07 / 07_地點/藥草鋪 / 08 / 99 |
| 2026-08-16 | 01§3-1 | `relations` 四維查完引擎消費者：只有 `trust`（`persuade` 成功率修正）合格，`affinity`／`familiarity`／`debt` 從沒被任何公式讀取過，只被寫入、餵給 AI 看，違反《00》原則三「沒有引擎消費者的欄位不留」，三項整個拿掉；AI 對一個人有沒有好感、熟不熟、欠不欠人情改交給《03》記憶系統自己記、自己判斷、自己演。連帶修正：`give`（送禮）不自動改變 `relations`（不管哪一維），理由跟 `haul` 一樣——送禮的真實意圖可能天差地遠，不該引擎蓋章；「新認識的人初始 affinity = reputation×0.2」隨 affinity 一起移除，改成 reputation 附中文形容詞讓 AI 自己判斷初始觀感；`filthy` 原本「他人 affinity 增幅減半」的效果失去掛靠欄位，且原設計本身是別人不知情下被引擎默默調整關係，另開 **P-35** 重新設計；全庫掃描發現 `report` 累積次數不驗證真假就強制拘留是同類問題，`report` 未實作先登記 **P-36**，等《洗心革面所》一起做 | 你 | 00 / 01 / 01-3 / 02 / 04 / 06 / 11 / 13 / 99 |
| 2026-08-16 | P-27 收尾 | `haul`／`struggle` action id 命名確認維持不變：跟現有 action id 風格一致（單一小寫英文動詞），全庫比對沒有撞名或保留字疑慮。拍板那一輪的 A/B/C/D 四個問題全部處理完畢，暫存筆記按慣例移除 | 你 | 99 / 07 |
| 2026-08-17 | 15 新增 | 新增《15 遊戲內 UI 規格》，把《13》§三點名的三個 MVP-1 in-game UI 定義完整：天神之石輸入框（點石頭開啟、不署名、40 字、送出後石頭本體冒氣泡 5 秒）、今日摘要（`today_plan` 意圖與新增的 `today_log` 行動紀錄兩欄並列，引擎不做兩者對照）、身上物品（36 格唯讀，觀察用不給操作）。三者併成同一個「角色資訊面板」的三分頁，理由是 640×360 放不下三個並存的置中面板、三者共用同一套選取機制。同時定下全域佔位規則（未接上的欄位顯示 `—`）與「面板資料一律不回頭注入 prompt」（否則等於繞過《03》記憶架構偷渡完美記憶）。P-20 #3（內心視窗欄位）的 MVP 子集隨之確定 | 你 | 15 / 99 / Ailley |
| 2026-08-17 | 05 補完 | 《05》補上兩塊 MVP UI 細節：§3-1 決策來源欄位（兩段式，先選 Local／Cloud／Human 再選模型；下拉只讀 `AIConfig` 已設好的 provider，面板不收金鑰也不寫設定檔；未選模型擋下存檔；一鍵隨機不隨機這欄，因為雲端會產生帳單）、§5-0 造型組挑選（6 選 1、必選不預選、佔位期顯示編號與色塊而非同圖換色，展開後即是既有的 `appearance[]`，不新增欄位） | 你 | 05 / 99 |
| 2026-08-17 | issue #177 | 《00》新增原則四：心智判斷類行為（結果取決於「另一個角色的心智要不要被說動／相信／感動」）交給當事人的模型自己判斷，不由引擎擲骰，跟原則一（技能／體力／運氣類）並列，不是其例外。`persuade` 依此改寫：不擲骰，比照事實句機制等下次自然決策一起帶給被說服者、不額外呼叫；模型輸出 `persuaded` 欄位，比照 `believed` 不驗證、不二次判定。適用範圍限逐一列名的行為，未列名者維持原則一；`perform` 等其他行為的全庫掃描另開 **issue #193（P-39）**，不在本次範圍內 | 你 | 00 / 01-2 / 01-3 / 04 / 11 / 99 |
| 2026-08-17 | issue #130 | 狀態總表「負責」「期限」兩欄合併改為「issue」欄：已定案項目填 `—`（決策已完成，無待追蹤 issue）、仍待規劃且有對應 issue 者填 issue 編號（P-26→#145、P-34→#174、P-39→#193），其餘留空；不逐項幫 P-01~P-38 剩餘待規劃項目開新 issue（見 issue 本文範圍界線） | 你 | 99 |
| 2026-08-17 | issue #136／P-19 | 確認《09》死亡屍體與墓園大半不適用於 MVP：§9 列出的 8 項待確認全部屬於「真正死亡」流程細節，MVP 用昏迷取代死亡（見《13》§三、P-27），不阻塞 MVP 開發；連帶發現狀態總表 P-20 #3 仍寫著已關閉的 issue #140，同步改成 #3 僅 MVP 子集已定（見《15》§2-7，完整三級欄位清單仍待完整版）、#5 已定案（見《10》§6.1） | 你 | 09 / 99 |
| 2026-08-17 | issue #186 | 《10》§9.1 恆公開欄位表移除「對在場者的好感度」一列：`affinity` 已於 2026-08-16（01§3-1）隨 `relations` 三維一起拿掉，現在只剩 `trust`，且《15》§2-7 已確認 MVP 不做關係網 UI，沒有東西可顯示，故整列拿掉而非改顯示 `trust` | 你 | 10 / 99 |
| 2026-08-17 | issue #158 | 開發 #158 時發現內文的 `persuade` 做法仍是 PR #195 拍板前的舊版擲骰公式，跟已定案的原則四／《01-2》§3-1 不一致；而 §3-1 要求的待回應事實句機制（記錄、下次決策注入、回應後清除）＋ decision schema 的 `persuaded` 欄位，程式裡完全沒有地基，屬於獨立新機制而非小工作。拍板：#158 縮小範圍只做 `give`／`shout`，`persuade` 拆出另開 **issue #227（P-40）** | 你 | 99 |
| 2026-08-17 | issue #193（P-39） | 心智判斷類行為全庫掃描完成：《01-2》§3、《11》§1 逐項核對，除 `persuade` 外沒有其他行為改分類。技能／體力／運氣類（`hunt_small`／`hunt_large`／`gather`／`fish`／`steal`／`struggle`）維持擲骰；`attack` 不套用擲骰，MVP 依 P-28 直接判定必中，跟心智判斷類無關，是閃避／格擋機制還沒實作的緣故（見《01-2》§3 註，2026-08-18 同步修正本檔與《01-2》的殘留矛盾，含《99》P-04 表格漏改的 attack 50% 舊值）；`talk`／`give`／`shout`／`murmur`／`report` 不適用（無成敗判定點或機械式門檻觸發）；`perform` 是唯一候選但觀眾不特定、無單一可問心智的對象，且尚未實作、無真實場景可驗證設計，維持擲骰，等真正實作有具體多人互動場景時再重新評估 | 你 | 01-2 / 11 / 99 |
| 2026-08-17 | P-22 #4（issue #245） | 《12》§8 簡化路線（`response_format`＋`supports_json_schema`）維持不變：§7.1 原本規劃的自建 `schema_to_gbnf()`／`/completion` GBNF 路線從未實作，原訂「手寫 GBNF vs schema 轉換 GBNF」比較做不了，改測現行路線本身的可靠度——GBNF 強制／純 prompt 約束兩組各實測 **50 次**，GBNF 強制 50/50 全過零失敗，純 prompt 約束 47/50（3 次 `action_not_allowed`，模型自己生成白名單外的 action，被 layer 3 正確擋下），兩組都沒出現社群回報過的 json_schema/grammar 衝突錯誤；`AISchema.validate_tasks()` 的合成壞資料 fallback 與 `_decide_with_retry()` 的重試路徑一併實測通過；過程中另觀察到一次 `Exponent too high` 數字解析警告（main 未併入 #224 防護時的離譜大數值，印證 #224 的必要性）。「同一 action 配不合理 duration」這類語意合理性這輪未測——分支未併入 #224 的 prompt 量級指引，測了只會重現已知問題，留待 #224 併入後再驗 | 你 | 12 / 99 |
| 2026-08-18 | issue #290／#268 | 拍板：`expires_at` 改用相對時長（模型填的 schema 欄位改名 `expires_in_minutes`），不維持絕對值語意——理由是不要求模型做「day×1440＋hour×60＋minute＋偏移」這種複合算術，本地小模型算錯的機率明顯較高，且不用曝露 `GameClock.day` 給模型。任務被接受進池子那一刻才換算成絕對 `expires_at`（`now_minutes + expires_in_minutes`），跟既有 schedule／debug 任務用同一套換算模式，不是新發明的例外。#269（`AISchema` 的邊界檢查／schema）依此改寫：邊界從動態偏移量（`now_minutes + offset`）換成固定常數（`MIN/MAX_EXPIRES_IN_MINUTES`），`plan_response_schema()`／`build_plan_envelope()` 不再需要吃 `now_minutes`。查證過程中另外發現 **P-47**：`GameClock.hour`／`minute` 沒進存檔（只存 `day`），任務池目前也完全沒被存檔，這個缺口暫不影響現況，但以後任務池接存檔時要一併處理 | 你 | 12 / 99 |
| 2026-08-19 | issue #141 | 拍板：商鋪「收購」機制先不做，維持純販售——跟現有 MVP 決策一致（生產鏈不在 MVP 範圍，角色沒東西可賣）。**不是承諾未來一定會做**，方向留白，等生產鏈（打獵/採集/釣魚地點與行動）真的要設計時再重新評估要不要加，不預先訂路線圖 | 你 | 08 / 99 |
| 2026-08-19 | issue #281 | 排程觸發的 `talk`（`npc_schedule.json` 目前僅 npc006 09:00、npc003 10:00，都在涼亭）從不帶 `params.target`，`_pursue_talk_task()` 原本直接拿空字串查名字，保證找不到人，排程觸發的 talk 永遠講不了話。拍板：只在 `params.target` 是空的（＝排程來源）時走 fallback，改為「抵達地點後，在自己 `TALK_RANGE`（32px）內的角色中選距離最近的一個」；`params.target` 有值時維持原本的依名稱查找分支不變。判定範圍是與自己的距離，不是「同一個 `place` 欄位」——抵達地點後彼此本來就在附近，用距離比用地點名更直接。不採「排程資料格式加 target 欄位」——查證三個線索確認排程本來就沒有指定對象的設計維度：`npc_schedule.json` 資料格式沒有 target 欄位、涼亭（`loc_pavilion`）的地點定位是「聚集」而非排定好的一對一見面點（規格書另有獨立的「約定」機制才是這種情境，見《10》）、《技術/talk 動作設計》只列了 debug 主控台與 LLM 決策會明確指名對象，排程從不在清單裡。新增 `_find_nearest_character_within()`，找不到人時視為「還沒人到齊」安靜等下一分鐘，不當成設定錯誤洗 log | 你 | 11 / 99 |
| 2026-08-19 | issue #290 | 查證發現 #290 本文的三件事只做了兩件：`expires_at` 換相對時長（拍板見上一列）已做、`prompt_builder.gd::PLAN_SYSTEM_TAIL_TEMPLATE` 補上 `expires_in_minutes` 的範例與說明（三層防禦的第二層）原本沒做，這輪補上；曝露 `GameClock.day` 給模型這件事本身因為改走相對時長設計已經不需要，不用另外做。剩下「`expires_in_minutes` 要不要收進 `required`」拍板：**維持選填**——已有安全預設值（省略時退回 `now_minutes + MAX_EXPIRES_IN_MINUTES`，不會退化成永不過期），且這個欄位只在「有時效性的機會」（例如约定未來要做的事）才有意義，跟 `priority`／`duration` 這種每筆任務都得想的必填欄位性質不同，改必填等於逼模型對不相干的情境也要瞎猜一個數字，增加整包回應被拒的風險，換到的好處（防止該填卻忘了填）在有安全預設值兜底下並不划算 | 你 | 12 / 99 |
| 2026-08-19 | issue #227 | `persuade` 機制拍板：發起 params 加選填的 `proposed_task`，有填是「行動說服」（`persuaded: true` 時機械式推進被說服者任務池，跟一般 LLM 任務同一套驗證）、沒填是「純思想說服」（`persuaded: true` 時連同模型自己判斷的 `importance`／`valence` 寫進 `Memory.add_candidate()`），由這個欄位有沒有填自然分流，不另設類型欄位。目標已有待回應記錄時新的說服直接判定失敗（比照 `talk_to()` 的 `TARGET_BUSY`），不覆蓋、不排隊，避免舊記錄被靜默蓋掉。範圍界線新增：說服目標是玩家（沒有 LLM 決策迴圈）不在這套機制內，另開 **issue #305** 追蹤。查證過程中另外發現 **P-49**：《04》描述的協定（`protocol_version`/`decision.action`...）已被《12》§8 簡化路線取代，欄位語意仍可參考但 JSON 形狀對不起來。補充 `proposed_task` 的格式分流契約（CodeRabbit review 抓到原本沒寫清楚）：省略 `proposed_task` 走純思想說服；`proposed_task` 通過 `AISchema._validate_task_shape()` 走行動說服；`proposed_task` 是 `null`、空物件、或驗證失敗——整個模型回應（`validate_tasks()` 的回傳）判定失效，不會降級成純思想說服；巢狀 `persuade`（`proposed_task.action == "persuade"`）在 `_validate_persuade_params()` 就直接判定失效，同一套「整包拒絕」邏輯 | 你 | 01-2 / 01-3 / 99 |
| 2026-08-19 | issue #227（CodeRabbit review） | PR #317 第三輪審查抓到兩個實作跟拍板不一致的地方：① `persuade` 送達（登記待回應記錄）後原本立刻呼叫 `_finish_task_and_request_next()`，完全沒吃到 P-09 拍板的 10 分鐘 duration——改成送達後佔住任務、交給 `_reevaluate_once()` 既有的通用「llm 任務 duration 走完才收尾」機制處理，跟 `gather`／`hunt_small` 同一套用法，不是 `give`／`shout` 那種送達即結束的一次性動作。② 已接受的 `proposed_task` 推進任務池時，若對方任務池已滿，`_push_llm_tasks()` 只會 `push_warning` 靜默丟棄——此時 `_pending_persuade` 已清空，沒有「下次再問」這條退路，一筆已經對目標說過「你被說動了」的承諾會憑空消失。新增 `_evict_lowest_priority_llm_task()`，池滿時先擠掉分數最低的既有 llm 任務再推進，保證已承諾的說服塞得進去 | 你 | 99 |
| 2026-08-19 | issue #227（CodeRabbit review） | PR #317 第四輪審查抓到三個問題，均已修復：① 純思想說服的 `importance` 欄位越界時，`validate_tasks()` 原本整包 `_fail(ERROR_BAD_SHAPE)`，跟 `validate_reflection()` 對同一種主觀分數欄位的處理（`clampi()` 夾制、不拒絕）不一致——`importance` 越界不是安全問題，改成同樣夾制，避免模型答對其他所有欄位卻因為這一個分數被整包重來，讓已清空的 `_pending_persuade` 沒有補救機會。② `_evict_lowest_priority_llm_task()` 淘汰的剛好是 `_current_task` 時，只清了 `_current_task`／`current_place`／`current_state`，沒有比照 `_finish_task_and_request_next()` 呼叫 `stop_moving()`、清 `_pursued_place`、重設 `_pursuit_done`——角色會繼續走去一個已經不存在的任務地點，之後仲裁器選到同地點任務時還會被殘留的 `_pursued_place` 誤判成「已有結論」而不重新 `move_to()`。③ `_resolve_pending_persuade()` 把 `try_record_pending_persuade()` 存的 `persuader`（顯示名）直接塞進 `memory.add_candidate()` 的 `related_npcs`，但 `Memory.add_candidate()` 這個欄位存的是 `character_id`，不是顯示名——新增 `persuader_id` 跟 `persuader` 分開存，`related_npcs` 改用 `persuader_id` | 你 | 99 |
| 2026-08-19 | issue #270 | 《08》§3-2 飲品表補上腐壞速率欄位：`water` 改判定為不會腐壞（`is_perishable` 由 1 改為 0，而非留著「標記會腐壞但 decay_rate=0」這種跟本則 issue 起因同一類的矛盾狀態），`ale` 腐壞速率定為 `+0.3`（介於《08》§3-1 食物表 0.3~0.8 區間下緣，液體保存期比熟食長）。`spirit` 不在 MVP 上架清單內，留待完整版一併決定。同步修改 `DatabaseSeeder.gd::ITEM_BALANCE` | 你 | 08 / 99 |
| 2026-08-20 | PR #367（CodeRabbit review） | PR #367 最新一輪 review 的 9 條 actionable + 2 條 nitpick，總計 11 條，全部指向這個「純搬移」PR 沒動過的既有程式碼（CodeRabbit 把整批搬移檔案當新檔案重新審查）。逐條比對規格書與程式碼分類：**4 條確認是真漏洞**，已在本 PR 一併修掉——`CharacterStatePersistence.gd` 角色節點釋放後補上 `tree_exited` 清理 `_inventory_connected`／`_loaded`／`_loading`／`_sync_pending` 字典；`_save_inventory()` commit 失敗分支補上 `rollback_transaction()`；`DatabaseSeeder.seed_items()`／`seed_all()` 改回傳 `bool` 並向上傳遞失敗狀態；`NPCSchema`／`NPCStateSchema` 的 `npc_id` 改成 `TEXT NOT NULL PRIMARY KEY`。**1 條規格書沒拍板**，登記 **P-50**（`grave.npc_id` 外鍵該指 `npc` 還是 `npc_death`）。**其餘 5 條**屬於重構／測試品質建議，非漏洞，未處理。**1 條**（`DatabaseSchema.gd` 的 `MIGRATIONS` 常數寫法）查證後確認 CodeRabbit 判斷有誤——既有寫法是刻意繞開 GDScript parser 對 const 陣列內 block lambda 縮排的 Parse Error，程式碼內已有註解說明，不用改（4+1+5+1＝11，數量對得上）。修 `npc_id NOT NULL` 時另外發現一個不在原本 11 條裡、但實作時浮現的問題：`CREATE TABLE` 只對新資料庫有效，既有資料庫要套用同樣約束得重建整張表，而近 20 張表都有 FK 指向 `npc(npc_id) ON DELETE CASCADE`——SQLite 的 `DROP TABLE` 在 `foreign_keys=ON` 時會觸發隱含 `DELETE` 讓 `CASCADE` 連鎖跑過所有子表，直接砍表會把子表資料連鎖刪光，這個 migration 需要拆出獨立 transaction 才能安全做，另外登記 **P-51** | Wendy | 99 / `CharacterStatePersistence.gd` / `DatabaseSeeder.gd` / `DatabaseManager.gd` / `NPCSchema.gd` / `NPCStateSchema.gd` |
| 2026-08-20 | PR #367（CodeRabbit review 第二輪） | `git mv` 讓整批搬移檔案在 diff 上等於新增，CodeRabbit 對同一批「純搬移」程式碼再跑出一輪新 finding（6 條 actionable + 1 條 nitpick），跟第一輪性質相同，逐條核對後全部處理完畢：**1 條 nitpick 採納**——`NPCScheduleSchema.gd` 比照 `GraveSchema`／`MemorySchema` 既有慣例補上 `idx_npc_schedule_npc` 索引。**1 條同類擴大採納**——`WorldSchema`／`LocationSchema`／`ItemSchema`／`NPCLastActionSchema`／`NPCAppearanceSchema`／`NPCOccupationSchema`／`NPCEmotionSchema`／`NPCGoalSchema`／`MemorySchema` 共 9 個 `TEXT PRIMARY KEY` 欄位缺 `NOT NULL`，跟 P-51 原本的 `npc_id` 問題同一個成因，新資料庫直接補上 `NOT NULL`；既有資料庫的重建 migration 套用 P-51 已拍板的理由一併回絕（`world`／`location`／`item`／`memories` 都有其他表 `ON DELETE CASCADE` 依附，砍表會連鎖刪資料；`DatabaseSchema.initialize()` 單一 transaction 裡切不了 `PRAGMA foreign_keys`），P-51 範圍同步擴大到全部 11 張表。**3 條確認是真漏洞，已修**：`CharacterStateIntegrationTest.gd` 補上資料庫就緒等待逾時（300 frames）＋抽出 `_restore_stats()` 讓所有提前結束路徑都還原被測試值覆蓋的真實 Character Stats（原本 `persistence` 找不到／`sync_now` 失敗／查無 `npc_state` 三條路徑會讓玩家角色的 runtime 數值卡在測試值，不會自己復原）；`InventoryPurchaseIntegrationTest.gd` 同樣補上就緒逾時，且把「物品存不存在」的驗證改成跟 runtime `count_item()` 逐項比對數量（原本只驗證存在，`npc_inventory` 本來就有同 item 時測不出漏寫入），移除因此變成死碼的 `_db_has_item()`；`CharacterStateIntegrationTest.gd`／`WashHygieneIntegrationTest.gd` 的前置條件失敗（找不到 Character/Agent 或 Stats）改用 `_fail()` 取代 `push_error()`，修正原本會在有 `[ERROR]` 訊息的情況下仍印出 `... PASSED` 的矛盾結果。**1 條查證後判定是誤報，不採納**：`WashHygieneIntegrationTest.gd` 疑慮 `agent.last_move_target` 在 `stop_moving()` 後被清除或改變——查 `character.gd` 的 `stop_moving()` 實作只清 `_path`／`_path_index`／`_stuck_timer`／`velocity`，`last_move_target` 只在 `move_to()` 內被寫入，測試裡 `stop_moving()` 到還原用的 `move_to(last_move_target)` 之間沒有任何 await 或會呼叫 `move_to()` 的程式碼插入，讀到的值就是還原前那個，不受影響 | Wendy | 99 / `NPCScheduleSchema.gd` / `WorldSchema.gd` / `LocationSchema.gd` / `ItemSchema.gd` / `NPCLastActionSchema.gd` / `NPCAppearanceSchema.gd` / `NPCOccupationSchema.gd` / `NPCEmotionSchema.gd` / `NPCGoalSchema.gd` / `MemorySchema.gd` / `CharacterStateIntegrationTest.gd` / `InventoryPurchaseIntegrationTest.gd` / `WashHygieneIntegrationTest.gd` |
| 2026-08-21 | issue #446 | P-55（原 P-51）既有資料庫 `NOT NULL` 主鍵 migration，先做 `memories`／`npc_appearance`／`npc_last_action`／`npc_occupation` 這 4 張目標主表（另外連帶重建 `memory_related_npcs` 這張關聯表，共 5 張 SQLite 實體表）——這幾張最多只有 1 張依附表，不需要 P-55 原本擔心的「拆 transaction 關 FK」那套，改用「改名成暫存表 → 對應 `*Schema.create(db)` 建回原名新結構 → 搬資料 → 依子表先、父表後順序砍暫存表」即可安全重建，`DatabaseSchema.gd` 新增 migration 版本 3。`npc`／`npc_state`／`world`／`location`／`item`／`npc_emotion`／`npc_goal` 這剩餘 7 張表（尤其近 20 張依附表的 `npc`）複雜度高很多，P-55 保留待決策 | Wendy | 99 / 技術/存檔 / `DatabaseSchema.gd` |
| 2026-08-22 | issue #426 | 事實句 `location_id` 反查機制落地：`places.gd` 新增 `resolve_from_position()`（世界座標 → 落在哪個地點錨點半徑內，都不在回傳空字串），`agent.gd` 新增 `_resolve_actual_place()` 包一層（半徑 32px，跟 `TALK_RANGE`／`WORK_RANGE` 等既有互動距離門檻同一個值）。只換掉 `_on_attacked()`（`_place_before_interrupt` 快照）與 `exit_conversation()` 這兩個 `current_place` 當下不可信的呼叫端；`current_place` 本身、`_memory_block()` 篩選端的比對基準都不動——見《記憶與睡眠反思》確認過這不會讓寫入/讀取兩側的地點語意兜不起來 | Wendy | 99 / 技術/記憶與睡眠反思 / `places.gd` / `agent.gd` |
| 2026-08-22 | issue #336 | 落地長動作固定間隔檢查點機制（《02》§3）：`agent.gd` 新增 `LONG_ACTION_CHECKPOINT_INTERVAL`（10 遊戲分鐘，跟既有 `MIN_ACTION_DURATION` 取同一個值，理由見《02》§3），`_reevaluate_once()` 在 llm 來源任務進行到一半、`elapsed` 每滿一個間隔倍數時觸發 `_request_checkpoint_decision()`——問「繼續」或「放棄」的輕量是非題信封（`PromptBuilder.build_checkpoint_envelope()`／`AISchema.validate_checkpoint()`），不是完整重新規劃；問到「放棄」交給 `_abandon_task_from_checkpoint()`，共用既有 `_finish_task_and_request_next()` 收尾。同步補上《02》§3 2026-08-16 擴充要求的「檢查點不限於發起者」介面：`character.gd` 新增 `get_checkpoint_dependents()`（目前唯一情形是被自己 haul 的目標，讀既有的 `_hauling_target`）與 `on_dependent_checkpoint()`（基底 no-op 掛勾），檢查點觸發時一併通知依附者，但**不**實作 haul 執行層或 struggle 判定本身——那是 #355／#337 的範圍，這裡只保證介面設計得出、接得上。P-14 #9 的檢查點間隔數值缺口本則補齊，`work` 進行時間缺口仍留給 #358 | Wendy | 02 / 99 |
| 2026-08-22 | issue #505 | 排程來源任務失敗後每個遊戲分鐘瘋狂重試（實測 `eat` 排程沒食物時連續噴幾十筆 `NO_FOOD`）：`agent.gd` 新增 `_mark_schedule_retry_backoff()`，失敗時把 `_retry_blocked_until`（window 結束的絕對分鐘數）掛在任務 Dictionary 本身，`_reevaluate_once()` 選候選時跳過還在退避的任務。只套用 `_pursue_eat_task()`／`_pursue_drink_task()`（硬失敗，不會在窗期內自己變好）；`talk` 排程刻意不套（等人到齊／等對方不忙會自己好轉，套上退避反而可能錯過時機）。根因（NPC 買不到食物）仍留給 #340 | Wendy | 99 / 技術/行程佇列與任務仲裁 / `agent.gd` |
| 2026-08-22 | issue #512／#513（P-50／P-54） | P-50 拍板：移除 `NPCDeathSchema.gd`／`GraveHighlightSchema.gd`——`SqliteSaveService` 完全沒有函式讀寫它們，也沒有其他 schema 用外鍵指向它們當父表，純粹死碼；`GraveSchema.gd` 保留，`grave_epitaphs` 的外鍵需要它當父表。`DatabaseSchema.gd` 新增 migration 版本 5（`DROP TABLE`）。P-54（`grave.npc_id` 該指 `npc` 還是 `npc_death`）隨 P-50 移除 `npc_death` 表而自然消失，`grave.npc_id` 維持指向 `npc(npc_id)`，「墓碑只能屬於已死亡角色」這層約束目前**尚未由資料庫或程式強制**，待死亡流程（#368）落地時才由應用層保證 | 你 | 99 / 技術/存檔 / `DatabaseSchema.gd` |
| 2026-08-23 | P-11（全專案盤點查證） | `/decide`／`/event` 逾時秒數現況跟 P-11 原決定（5/15/8 秒分開）不符：`Ailley/scripts/ai/ai_config.gd` 的 `DEFAULT_TIMEOUT` 是單一全域 10 秒，`LocalLLM`／`RemoteLLM`／`/event` 共用。程式碼註解已說明是刻意設計（`HTTPRequest` 節點池共用，timeout 是節點屬性不是逐請求參數，做成逐 provider 不同值不會真的生效），不是遺漏，只是規格書沒有回填這個實作限制。《04》§6 已同步；P-11 標題保留「已定案」，內文加註現況已不同，不當成新的待決策問題——除非之後真的需要逐 provider 不同逾時，屆時要先解掉「timeout 是節點屬性」這個技術限制，不是改個數字就好 | AI盤點（讀 `ai_config.gd` 程式碼註解得出） | 04 / 99 |
| 2026-08-23 | P-17 #13-15 | Stage 1 決策輪三題：6 個地點容量數字（餐酒館8／涼亭6／森林4／藥草叢4／洗心革面所3／結婚禮堂8）拿掉【推測】直接定案；村莊廣場獨立開新地點 `loc_square`，不併入涼亭的「聚集」用途；布告欄跟《10》§9 B35 系統自動記事確認是同一個東西，身兼玩家留言板與系統事件記錄兩用途。地點總數由 15 增為 16 | 你 | 07 / 07_地點/餐酒館、涼亭、森林、藥草叢、洗心革面所、結婚禮堂、村莊廣場、布告欄 / 10 / 99 |
| 2026-08-23 | P-17 #10 | 拍板：遊戲永久只有一個村莊，不會有第二個——PR #546 審查時發現[[布告欄]]「留言板（兩村各一）」跟《07》§4／P-17 #10「MVP 不適用、完整版才決定」兩處敘述互相矛盾，且都預設未來會有第二村。改成直接判定「不適用」，不再留完整版的懸置選項：《07》文件開頭 callout、§1 地點清單第 10 列、§4 跨村移動列、[[布告欄]]用途說明全部拿掉「兩村各一」與「完整版才決定」的敘述 | 你 | 07 / 07_地點/布告欄 / 99 |
| 2026-08-25 | P-63/64/65/66/67/68／P-62 #6／移動速度／衛生驅動 | 這一輪的完整記錄留在《99》各 P 編號區塊本文（拍板當下就寫進去，沒有另外壓縮成一行）：P-63（curiosity 不做探索權重公式）、P-64（偷竊警覺度改走 Vision／make_noise 偵測管線）、P-65（hygiene 拿掉被動漂移改工作驅動）、P-66（跟隨系統拉進 MVP）、P-67（對話開場白一律過 LLM）、P-68（六種玩家狀態小圖標）、P-62 #6（表演觀眾人數不做引擎公式修正）。移動速度 80→60（PR #569）、hygiene drift 0.5→0（PR #570）為實際程式碼改動 | 你 | 01 / 01-2 / 07 / 07_地點/深井、湖泊 / 11 / 17 / 99 / ai/api |
| 2026-08-25 | PR #557 收尾／舊 issue 盤點 | PR #557 跟 `main` 有 4 個真衝突（`Ailley.md`／`技術/昏迷狀態機制`／《09》／《17》，因 `main` 上死亡系統 PR #551／#555 同期合併），已解決；CodeRabbit 累積約 20 則 review comment 逐條核對，大半已被本 session 稍早的 commit 順手修掉，剩下真的還沒解的（`IMPLEMENTED_ACTIONS`白名單範圍說明、P-11逾時秒數表格）已修；P-59（`location_id`／`loc_`命名落差）維持待決策，未動。核對過程中一度誤把《99》P-17 #10「單一村莊永久拍板」改成「僅MVP限制」，反查決策紀錄後發現是既有拍板、已撤回改動。舊 open issue 盤點：#375（玩家死亡不對稱規則）關閉——依賴的《10》§8.3 世界封鎖規則本身無實作也無 issue，等§8.3落地再重開；#383（last_words 生成）關閉——查code發現已隨#379／PR#551實作完畢，issue沒同步關；#156／#387／#386補上assignee；#531（sandbox.tscn）從W改指派C（專案環境整理屬她的角色）；#492（AIService優先序）「phase2前完工」的期限框架已跟新MVP攤平清單不符，加comment說明併入一般排序不再特別趕 | 你 | 99 / 17 / CLAUDE.md / 07 / ai/決策紀錄 |
| 2026-08-27 | P-62 #5／P-39 補充 | PR #557 CodeRabbit review 揪出兩處矛盾：（1）《17》已寫 perform「隨地表演」但《99》P-62 #5 仍列未決——拍板：隨地表演，不做涼亭限定，同步改《07_地點/涼亭》移除「限定本地點」敘述；（2）《08》`instrument` 定義「演奏成功率 +15%」，但《01-2》perform 權威公式完全沒提。追問後發現更根本的問題：perform 是持續進行的長動作（跟打獵/採集/偷竊的單次判定不同），原公式沒處理「怎麼判定持續中的動作」。拍板：perform 依《02》§3 長動作檢查點機制，每個 checkpoint 各自重新擲一次成功率，段與段結果獨立；`instrument` 在每段重擲時都疊加 +15%（門檻道具＋數值加成兩者並存，不是二選一）| 你 | 01-2 / 07_地點/涼亭 / 11 / 99 / ai/決策紀錄 |
| 2026-08-25 | 團隊分工開 issue（#571-585）／多人連線排除確認 | 依《17》MVP清單比對35則open issue，找出15個完全沒有issue在追的缺口，逐則開新issue（英文標題中文內文），依team分工assign（每則都有assignee，決策類優先給R）；P-57（墓園動態擴張）2026-08-25再次確認不進這學期MVP，移到未來願景，維持《09》§6簡化版（滿格拒絕新安葬）作為這學期最終行為。**多人連線／連線功能明確不拉進這批**——粗估最小可行版本（伺服器技術棧決定、WebSocket連線/斷線/重連、世界狀態同步、`RemotePlayer` DecisionProvider落地、連線大廳UI、多client測試環境）約需8-12則新issue，且跟現有15則性質不同：現有15則大多「地基已在、缺一段接線」，連線這批是從零開始沒有任何既有基礎，粗估整批工作量是目前15則總量的1.5-2倍、風險也更高（沒人事先做過WebSocket/伺服器），維持排在未來願景不拉進這次專題發表範圍。指派修正：N（TianChiChan）專攻AI邏輯，#573（打獵，動物節點明講不是LLM決策架構、AI部分只是白名單加一行）與#583（自動化測試覆蓋，橫跨多個非AI系統）改指派給T，避免把非AI領域工作算進N的專攻範圍 | 你 | 17 / 99 / ai/決策紀錄 |

## 相關

- [[99_待規劃項目清單]] —— 目前還未拍板的 P 編號，狀態總表
