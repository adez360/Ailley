---
tags:
  - ai
status: 參考
updated: 2026-08-08
---

# changelog

AI 專用。**筆記庫裡唯一可以寫「原本是什麼」的地方。**

其他地方（`技術/`、`ai/api.md`、`交流/`）一律只寫現況：東西改了就把舊的那段刪掉，
不是在旁邊加註。被刪掉的決定如果日後可能有人再提一次，就記到這裡 ——
查資料的人不會讀到過期內容，吵完的事也不會繞回來。

## 記什麼

```
記   決定被推翻      （原本 X → 現在 Y → 為什麼換）
     方案被否決      （考慮過 X，因為 Y 不做）
     整篇被刪的筆記  （它裝過什麼、結論搬去哪）
不記 新增功能、修 bug、重構   → git log 就夠了
     還沒拍板的討論           → 那在 `交流/`
```

一則就是一個決定的來歷，日期倒序。**不要寫成每日工作日誌。**

---

## 2026-08-08

### 取消主模式／AI 模式的區別

- **原本**：`主模式`（玩家操作）與 `AI模式`（觀察 agent 自主生活）兩個模式，
  共用角色存檔與世界存檔；已拍板「AI 模式裡玩家角色不存在，不可讀 player 存檔」
- **現在**：不分模式。世界之間唯一的差別是裡面有沒有 player。
  世界存檔多一個建立時決定的旗標「**允不允許 player 加入**」，允許也不強制在場
- **為什麼**：整個區別化簡下來就只有「有沒有 player」，不值得做成兩套東西
- **消失的問題**：「AI 模式跑掉 3 個遊戲日再切回主模式，時間線怎麼銜接」——
  一份世界存檔就是一條時間線，且只在被開著時推進，沒有另一邊要銜接
- **浮上來的問題**：角色存檔跨世界共用 → **鎖要鎖在角色這一層**，不只鎖世界存檔
- **待拍板**：那個旗標建好之後還能不能改（傾向能改）

> [!warning] 別把「有沒有 player」寫成載入時算一次的分支
> 玩到一半有人離線就壞了。世界層問的是「允不允許加入」；
> 「現在有沒有人在操作」是角色層的問題。

### 刪掉 `交流/草案決策.md`

五題拍板問答 + 逐條評估。結論全數已在 `交流/計畫.md` 與 `交流/決策.md`，
而內文大量建立在已經不存在的模式區別上。過程紀錄，拍完板就不留。

### `技術/` 不再引用 `交流/`

21 處 `[[決策]]` / `[[計畫]]` / `[[Localization]]` 全部拆掉。
`交流/` 的筆記是刻意會被刪的（草案決策就是實例），連結會整批變死。
做法不是刪連結，而是**把被引用的結論改寫成該篇自己的句子**；
其中「什麼時候請 LLM 重排」的六個觸發情境整份抄進 `技術/行程佇列與任務仲裁.md`。

未處理：`note/Ailley.md` 的「交流」小節仍用 wikilink 指進 `交流/`，等人決定怎麼換。

---

## 這個檔案存在之前

沒有系統性紀錄。以下是原本散落在各篇筆記裡、因為「還在影響現在的決策」而被保留的
否決案，集中列出以免有人再提一次：

| 否決的方案 | 為什麼不做 | 現在的做法 |
| --- | --- | --- |
| Agent↔Agent 一次呼叫產生整場 6 輪對話（成本最低） | 多人時兩隻屬於不同玩家，等於某台 client 幫別人的角色寫台詞；而且會分岔成「本機批次、線上逐輪」兩套實作 | 一律逐輪，帳單自然落在正確的人頭上 |
| AI 重寫整張行程表，拒絕更新時回傳 `{"update": false}` | 改成 push 語意之後這個特殊值沒必要，還多一種 LLM 回錯格式的方式 | 不更新就是回傳空陣列 |
| 自架 WS 中繼幫玩家代打 LLM | 同時違反「費用由 Agent 擁有者負擔」與「本地模型跑在擁有者機器上」，還多一台伺服器要維運 | HTTP 直連 provider，跑在擁有者自己的機器 |
| 家園採非同步快照參觀、不做即時共玩 | 本遊戲不是競技類，防作弊優先度不高，不值得為它砍掉共玩 | 所有世界皆可即時聯機，主機為權威 |
| 交誼區由專用伺服器當權威 | 同上 | 主機為權威 |

再往前查 `git log`。2026-08-07 那次 `refactor: rebuild the village on a shared
Character base` 取代掉它之前的大部分東西，不值得考古。
