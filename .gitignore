# ── 第三方工具（本地 build 用，不進版控）─────────────
poc/llama.cpp/
llama.cpp/

# ── 模型檔（動輒數 GB，絕對不能進 git）──────────────
*.gguf
*.ggml
*.bin
*.safetensors
models/

# ── Python ──────────────────────────────────────────
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── 環境變數 / 密鑰（雲端 API key 千萬別進版控）──────
.env
.env.*
*.key
secrets.*

# ── Godot ───────────────────────────────────────────
.godot/
.import/
export.cfg
export_presets.cfg
*.translation
# 匯出的打包產物
build/
dist/
*.exe
*.pck
*.dmg
*.app/

# ── SQLite 本地資料庫（開發期資料，不共用）──────────
*.db
*.sqlite
*.sqlite3
*.db-journal

# ── 編輯器 / 系統雜項 ───────────────────────────────
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ── 日誌與暫存 ──────────────────────────────────────
*.log
tmp/
*.tm

# ── POC 生成的對話 transcript／角色名單／持久化記憶（每次跑都會新增，不進版控）──
poc/transcripts/
poc/characters/
poc_mode_a/transcripts/
poc_planning/transcripts/
poc_agent_loop/transcripts/
poc_agent_loop/memory_store/
poc_village_sim/transcripts/
poc_village_sim/memory_store/
poc_archive/poc/transcripts/
poc_archive/poc/characters/
poc_archive/poc_planning/transcripts/

# ── 各自的obsidian ──────────────────────────────────────
.obsidian/

# ── 內含機敏資訊（SSH 帳號／IP／port／遠端路徑）的私人筆記，不對外顯示 ──
note/30-技術架構/遠端 GPU 機器連線與 llama-server 操作手冊.md

# ── 第三方論文/文件（體積大、有著作權，只留本機參考）──────
town.pdf
GPT.pdf

# ── Ru 團隊規格書 Notion 匯出（私人/共用文件中心，不進版控，只留本機參考）──
私人和共用/
