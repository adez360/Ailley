class_name Memory
extends Node

## 角色的近期記憶（L2）與核心記憶（L4）。規格見 note/規格書/03_事件評估與記憶.md §2。
## L1（短期工作記憶）與 L3（長期向量檢索）這次不存——L1 開場即可重建，
## L3 MVP 不採用向量檢索，見 note/技術/存檔.md「記憶怎麼存」。
##
## 兩層都直接存整包陣列，不另外包查詢／寫入方法——目前還沒有任何地方真的
## 產生記憶（睡眠反思流程是後續 issue），這裡先備好存檔要讀寫的欄位；
## 走訪／驗證每一筆的規則等真正的寫入來源接上時再補（YAGNI）

var l2: Array[Dictionary] = []		# 近期記憶，不定筆數
var l4: Array[Dictionary] = []		# 核心記憶，上限 5 條（引擎尚未強制，見上）
